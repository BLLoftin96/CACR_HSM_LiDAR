---
title: "CACR LiDAR Habitat Suitability Modeling"
author: "Willow L. Loftin"
date: "2026-01-02"
output: html_document
---

# GitHub Setup

I am using GitHub as a form of version control, so I will need to connect this document to the repository on GitHub. While the version set up was already created in tandem to this document, RStudio will often require verification before you can push any committed changes to your code.

**This section is only needed for those modifying code**

```{r Git Setup, eval=FALSE, include = FALSE, message = FALSE, warning = FALSE}

# We will need the usethis package so that we can connect our GitHub

install.packages("usethis")
library(usethis)

# Using the usethis package we can tell R which user will be pushing new versions to the associated repository

use_git_config(user.name = "BLLoftin96", user.email = "brittanieloftin@u.boisestate.edu")

# This will show you who is logged in and will give you options following options: 1) Abort update with error, and keep the existing credentials, 2) Replace these credentials, and 3) See the password/token. If this is a fresh session, I would recommend the replace option and re-enter your current password/token to ensure there is no connection error. 

gitcreds::gitcreds_set()

# This will verify any changes

gitcreds::gitcreds_get()

```

# Background

The Cassia Crossbill (*Loxia sinesciuris*) is Idaho's only endemic species whose limited range and diet put the species at risk for extinction (*FIND CITATIONS FROM NWCASC ADVISOR GRANT RE: IDFWS AND CACR STATUS*; Parmesan 2006, Benkman 1993). Similar to other crossbills whose crossed mandibles aid in prying open the conifer cones of a single species (or a variety of species) to then forage on the seeds inside (Newton 1972, Benkman 1978a, Benkman 1978b, Benkman 1988a, Benkman 1988b, Benkman 1989, Benkman 1990, Benkman 1992, Benkman 1993, Benkman and Lindholm 1991). Cassia Crossbills feed almost exclusively on the serotinous cones of Rocky Mountain lodgepole pine (*Pinus contorta latifolia*) and are unable to survive competition with other crossbills for other seeds elsewhere (Benkman et al. 2001, Benkman et al. 2012, Benkman 2016). Unlike the far more common Red Crossbill (*Loxia curvirostra*) that are nomadic and able to travel vast distances for food, the Cassia Crossbill have a highly restricted range limited to the 67km2 of Rocky Mountain lodgepole pine forest found on two small mountain ranges in Idaho: the South Hills and the Albion Mountains (Benkman et al. 2009, Behl and Benkman 2018; Figure 1). Despite lodgepole pine existing in other areas, the Cassia Crossbill was able to evolve in this region due to the lack of red squirrels (*Tamiasciurus hudsonicus*) which typically preemptively out-compete crossbills for cones (Benkman 1999, Smith and Benkman 2007).

![Figure 1: Map showing the South Hills and Albion Mountain range. The white outline marks the boundary of the Sawtooth National Forest](C:/Users/britt/OneDrive/Pictures/CassiaSites1.png)

The combination of being a permanent resident restricted to high elevation environments and dependent on a singular resource makes the Cassia Crossbill extremely sensitive to habitat alteration or loss (Benkman 1993). For example, in 2020 the Badger Fire swept through the western portion of the South Hills and destroyed approximately 20% of the available lodgepole in the region. In subsequent years, annual trends in the population declines significantly (*FIND CITATION FROM NW CASC GRANTS OR ANNUAL REPORTS & FIGURES*). Wildfires in this region -like many other areas in the western US - are predicted to increase in frequency and intensity in the future (Brice et al. 2020, Rogers et al. 2020). Wildfires can damage or destroy the mature stands of lodgepole pine which contain the serotinous cones - cones sealed in a resin - that Cassia Crossbills forage on. However, when these areas experience stand-replacing fires the resin on the serotinous cones melts and seeds inside have the potential to germinate and create a new stand. Serotinous cones in this region may also be affected by high summer temperatures which some evidence has shown to cause these cones to open prematurely and allowing other species to access a once exclusive resource (Benkman et al. 2001, Benkman et al. 2012, Santisteban et al. 2012, Benkman 2016).

# Goals

# Data

LiDAR data was taken from [existing data from the Idaho LiDAR Consortium](https://www.idaholidar.org/existing-lidar-data/) and utilizing 5 different projects.

1.  Elko NW (2021 imagery) - South Hills - 196,727 ACRES
2.  Southern ID 2018 #12 - South Hills (**Note: Southern ID 2018 #12 is stored as Part 21 and Part 22**)
3.  Southern ID 2018 #14 - E. Albions
4.  Southern ID 2018 #15 - South Hills (**Note: Southern ID 2018 #15 is stored as 07_Cassia**)
5.  Southern ID 2018 #17 - E. South Hills and W. Albions (**Note: Southern ID 2018 #17 is stored as LAZ**)

## LiDAR Flight Information

### Elko NW

### Southern ID 2018 \# 12

### Southern ID 2018 \# 14

### Southern ID 2018 \# 15

### Southern ID 2018 \# 17


# Install Packages

```{r Install Packages, message=FALSE, warning=FALSE}
# Install

install.packages('lidR')
install.packages('sf')
install.packages('lidRviewer', repos = c('https://r-lidar.r-universe.dev'))
install.packages('dplyr')
install.packages('terra')
install.packages('RCSF')
install.packages('future')
install.packages('future.apply')
install.packages('progressr')
install.packages('data.table')
install.packages("stringr")

# Load
library(lidR)
library(sf)
library(lidRviewer)
library(dplyr)
library(terra)
library(RCSF)
library(future)
library(future.apply)
library(progressr)
library(data.table)
library(stringr)
```

# Data Inspection

Originally, I was using lidRviewer package in R to visually inspect the areas to see if they matched areas I had been to. However, given my storage, it wasn't possible to load the subset of tiles fully without overloading my computer or R's memory. To resolve this, I am doing a quick look over of the tiles in RStudio to quickly get a sense of whether they look natural and if they match up with the area they are intended to represent. After, the full tile is removed so I can inspect the next one. Then the full tiles are loaded into CloudCompare (version 2.13.2) for a better quality assessment to check the density patterns, canopy penetration, and ground realism. Once tiles are assess, we can configure the pipeline for filtering, classification, and generating forest metrics.

**Note: code chunks are not set to run to avoid issues with memory. Rather code chunks should only display code for reproducibility. Additionally, screenshots of CloudCompare areas were saved to compare densities and for reproducibility.** Commands:

-   Rotate with left mouse button

-   Zoom with mouse wheel

-   Pan with right mouse button

-   Keyboard r or g or b to color with RGB

-   Keyboard z to color with Z

-   Keyboard i to color with Intensity

-   Keyboard c to color with Classification

-   Keyboard + or - to change the point size

-   Keyboard l to enable/disable eyes-dome lightning

Since I am using the raw point cloud, we are likely to see withheld and synthetic classified points. *Note: reasoning for withholding 'withheld' points:*

-   *they are points the data provider recommends you ignore*

-   *overlap in flight lines*

-   *edge-of-scan artifacts*

-   *points kept for QA but not analysis*

-   *points excluded from the official product*

*I am keeping the synthetic points because:*

-   *adds improved surface continuity*

-   *points are generated during processing*

-   *densified surfaces in workflows*

## Elko NW 2023

Based on the Elko flight reports they used 3 providers using 3 different LiDAR systems (see flight report for additional details) which had a max return per pulse of 7 to 8 (8 being the most common).

```{r Elko NW 2023 Viewer Inspection, eval=FALSE}

############### Father and Sons Camp (with logger) ################

# Load the Tile containing Father and Sons Campground
F_S_Camp_Elko = readLAS('R:/ElkoNW_LAZ/USGS_LPC_NV_NorthWestElko_2020_D20_11TQG320710.laz')

# Visually inspect
lidRviewer::view(F_S_Camp_Elko)  

# Imagery based on lidRviewer looks similar to what I have seen, very dense point cloud. Visually, I could only see 1 or two points that are a little above the tree line - probably a bird. CloudCompare offered better clarity appears very similar to what I have seen. Noticed some gaps in the ground level and based on the number of returns it appears that the highest is indeed 8.

################ Porcupine Springs (with loggers) ################

# Load the Tile 
P_S_Camp_Elko = readLAS('R:/ElkoNW_LAZ/USGS_LPC_NV_NorthWestElko_2020_D20_11TQG260710.laz')

# Visually inspect
lidRviewer::view(P_S_Camp_Elko)  

# lidRviewer shows 7 points that are way above the tree line. Points look very good and dense so I feel they look accurate to the ground. CloudCompare view looked very good and despite density I could see distinct separation of trees.

################ Logger MLPF-107 ################

# Load the Tile 
Log_MLPF107_Elko = readLAS('R:/ElkoNW_LAZ/USGS_LPC_NV_NorthWestElko_2020_D20_11TQG350815.laz')

# Visually inspect
lidRviewer::view(Log_MLPF107_Elko)  

# lidRviewer shows many points above the tree line and below ground level but looks densely packed

################ Loggers LPO-034 and LPO-130 ################

# Load the Tile 
Log_LPO034_LPO130_Elko = readLAS('R:/ElkoNW_LAZ/USGS_LPC_NV_NorthWestElko_2020_D20_11TQG305605.laz')# Visually inspect

# Visually inspect
lidRviewer::view(Log_LPO034_LPO130_Elko)  

# lidRviewer had maybe ~5 points that seemed off, but it looked very good. CloudCompare looked excellent and looked very similar to what we saw in the field.
```

Overall, lidRviewer seems to indicate the area looks visually valid. Looks like some areas may have some error points and the laser couldn't always get the entire way to the ground so there are holes in there.

## Southern ID 2018 \# 12

Southern ID 2018 #12 appears to have 4 different providers using 2 different systems of LiDAR. Notably the Galaxy PRIME system had a max of 7 - 8 returns while the VQ1560i had unlimited returns. CloudCompare view was excellent with the number of returns being similar to that noted in the flight plan (\~7 or 8).

```{r Southern ID 2018 # 12 Viewer Inspection, eval=FALSE}
################ Bear Gulch Campground ##############

# Load the Tile containing Bear Gulch Campground
B_G_Camp_12 = readLAS('R:/Part22/USGS_LPC_ID_SouthernID_2018_D19_11TQG155770.laz')

# Visually inspect
lidRviewer::view(B_G_Camp_12)

# liRviewer shows some points above the trees.CloudCompare was easier to check with trees being easier to see.

################ Diamondfield Jack Campground ##############

# Load the Tile 
D_J_Camp_12 = readLAS('R:/Part22/USGS_LPC_ID_SouthernID_2018_D19_11TQG230725.laz')

# Visually inspect

lidRviewer::view(D_J_Camp_12)

# lidRviewer shows many points above the treeline. Very dense location but can see some delineation of canopy but a bit harder to tell in this program. CloudCompare view was much easier to read. A bit harder to see the canopy delineation for the intensely packed LP

################ Unique (known) ##############

# Load the Tile 
known_loc_12 = readLAS('R:/Part21/Part21/USGS_LPC_ID_SouthernID_2018_D19_11TQG380725.laz')

# Visually inspect
lidRviewer::view(known_loc_12)

# lidRviewer shows many points above the treeline. Area has small densely packed stands by side of road but majority of land is open. It was hard to tell in this program about the areas with stands as controls are limited and you cannot chose which areas to zoom to which made it hard to assess canopy delineation. CloudCompare was a bit easier to see with trees looking distinguishable.

################ Logger LPO-030 ##############

# Load the Tile 
Log_LPO030_12 = readLAS('R:/Part22/USGS_LPC_ID_SouthernID_2018_D19_11TQG185710.laz')

# Visually inspect
lidRviewer::view(Log_LPO030_12)

# lidRviewer shows many points that are above the treeline. This region is probably the densest for the area, but it looked good from the quick check. CloudCompare appears to be what I expect. There is some very very densely packed trees here which may be hard to delineate.
```

## Southern ID 2018 \# 14

Flight appears to have hired 3 providers with 3 different LiDAR systems with 2 having unlimited returns and 1 having 9 max returns. There were no areas in this section that I had surveyed. Instead, I opted to use 3 locations of varying densities to see how they look.

```{r Southern ID 2018 # 14 Viewer Inspection, eval=FALSE}

################# ref 1 #############

# Load the Tile 

ref1_14 = readLAS('R:/SouthernIdaho2018_14_LAZ/SouthernIdaho2018_14_LAZ/USGS_LPC_ID_SouthernID_2018_D19_12TTM820755.laz')

# Visually inspect
lidRviewer::view(ref1_14)

# Denser portion of the study area appears to look good on the quick assessment, but there are points above the tree line that do not make sense.CloudCompare shows up to 4 or 5 returns but the density and the overall delineation looks good.

################# ref 2 #############

# Load the Tile 

ref2_14 = readLAS('R:/SouthernIdaho2018_14_LAZ/SouthernIdaho2018_14_LAZ/USGS_LPC_ID_SouthernID_2018_D19_12TTM835740.laz')

# Visually inspect
lidRviewer::view(ref2_14)

# area is side of mountain with one slope being mostly low level veg and other forested. Still see points above tree line. CloudCompare shows up to 4 or 5 returns but the density and the overall delineation looks good.

################# ref 3 #############

# Load the Tile 

ref3_14 = readLAS('R:/SouthernIdaho2018_14_LAZ/SouthernIdaho2018_14_LAZ/USGS_LPC_ID_SouthernID_2018_D19_12TTM850725.laz')

# Visually inspect
lidRviewer::view(ref3_14)

# least dense area --> pretty much all sagebrush and low trees. Some points above treeline. CC shows 4 returns and clearly shows the open area.
```

## Southern ID 2018 \# 15

Flight had 2 providers with the same LiDAR system both with unlimited returns. There were no areas in this section that I had surveyed and this area takes up the smallest area. Chose areas around river which appeared to have trees that I could assess. Max number of returns shows only 3 for ref1 and ref2, but area appears to have low vegetation so this may be the max number of returns needed.

```{r Southern ID 2018 # 15 Viewer Inspection, eval = False}

################# ref 1 #############

# Load the Tile 

ref1_15 = readLAS('R:/07_Cassia/USGS_LPC_ID_SouthernID_2018_D19_11TQG425815.laz')

# Visually inspect
lidRviewer::view(ref1_15)

# area is kinda what I expect with lots of sagebrush since its lower elevation with denser taller trees in the riparian zone. Still have floating points. CC shows points both above the treeline and below the ground. Appears to be an area of very low veg. Riparian area looks to have some gaps under the veg but this may be due to the shear slope that is in the area.

################# ref 2 #############

# Load the Tile 

ref2_15 = readLAS('R:/07_Cassia/USGS_LPC_ID_SouthernID_2018_D19_11TQG440830.laz')

# Visually inspect
lidRviewer::view(ref2_15)

# area is sparse and appears to be mainly sagebrush and we still see floating points. CC shows similar view with vegetation looking to be predominantly sage brush.
```

## Southern ID 2018 \# 17

Flight had 3 providers with 2 LiDAR systems of which the VQ1560i had unlimited returns while the Galaxy-PRIME had a max of 7. CC shows max return of 6.

```{r Southern ID 2018 # 17 Viewer Inspection, eval=FALSE}

############### Thompson Flat Campground ################

# Load the Tile 
T_F_Camp_17 = readLAS('R:/LAZ/USGS_LPC_ID_SouthernID_2018_D19_12TTM835890.laz')

# Visually inspect
lidRviewer::view(T_F_Camp_17)

# Area looks how we expect but lots of floating points. CC shows 6 max returns. Vegetation looks similar to the area on the ground with open patches and characteristic Douglas-Fir. Area has dense patches that may be hard to characterize under the canopy layer. 

############### Independence Lake Campground ###############

# Load Tile
I_L_Camp_17 = readLAS('R:/LAZ/USGS_LPC_ID_SouthernID_2018_D19_12TTM790770.laz')

# visually inspect
lidRviewer::view(I_L_Camp_17)

# super dense area, trees look identifiable, lots of floaters. CC shows max of 6 returns with very dense vegetation. Canopy layer may be hard to pierce but canopy trees do look like they may be delineated.

############### Logger LPO-060 #############
Log_LPO060_17 = readLAS('R:/LAZ/USGS_LPC_ID_SouthernID_2018_D19_12TTM745695.laz')

# visually inspect
lidRviewer::view(Log_LPO060_17)

# super dense area, trees look good, floating points and also some underground? CC shows max of 6 returns with very dense vegetation. Canopy layer may be hard to pierce but canopy trees do look like they may be delineated.
```

In general, we do see some points that float above the tree line as well as some points below ground (though to a much lesser degree). Notably, densely packed areas may be harder to delineate based on LiDAR used and I notice some empty space below the canopy where it looks like the laser couldn't quite make it to the ground.

Based on this information, we should reclassify these tiles. We should begin by working with these tiles to create a general pipeline for classification (which we can then use the **batch** function to do to all tiles).

# Pipeline Code (Clipped Tiles)

Here our goal is to create a pipeline by generating code using these 3 tiles. We should then be able to batch these to other tiles in the study area. Process steps:

1.  Noise Filter - start with this in case there are unusual noise below ground (may accidentally create weird geometry and makes ground classification more stable)

2.  Classify Ground Points

3.  Second cleanup of noise - if needed

4.  Generate a Normalized Canopy Height Model


## Create Catalog for Pipeline

**To do this step and save memory, we are going to create a small catalog of our earlier tiles to represent different densities, slopes, and regions in order to test whether the pipeline fails to accommodate any of these things. We will use the following 8 tiles:**

-   *Logger LPO-034 & Logger LPO-130 - Elko*

-   *Porcupine Springs Camp - Elko*

-   *Diamondfield Jack Camp - Southern ID 2018 #12*

-   *Known location - Southern ID 2018 #12*

-   *Reference 3 - Southern ID 2018 #14*

-   *Reference 2 - Southern ID 2018 #15*

-   *Independence Lake - Southern ID 2018 #17*

-   *Logger LPO-060 - Southern ID #17*

```{r Create Pipeline Catalog}

# Create a path to the files 

pipeline_files <- c(
  "R:/ElkoNW_LAZ/USGS_LPC_NV_NorthWestElko_2020_D20_11TQG260710.laz",
  "R:/ElkoNW_LAZ/USGS_LPC_NV_NorthWestElko_2020_D20_11TQG305605.laz",
  "R:/Part22/USGS_LPC_ID_SouthernID_2018_D19_11TQG230725.laz",
  "R:/Part21/Part21/USGS_LPC_ID_SouthernID_2018_D19_11TQG380725.laz",
  "R:/SouthernIdaho2018_14_LAZ/SouthernIdaho2018_14_LAZ/USGS_LPC_ID_SouthernID_2018_D19_12TTM850725.laz",
  "R:/07_Cassia/USGS_LPC_ID_SouthernID_2018_D19_11TQG440830.laz",
  "R:/LAZ/USGS_LPC_ID_SouthernID_2018_D19_12TTM790770.laz",
  "R:/LAZ/USGS_LPC_ID_SouthernID_2018_D19_12TTM745695.laz"
  ) # Order: Porcupine Springs Camp, Logger LPO-034 & Logger LPO-130, Diamondfield Jack Camp, Known location (SID 2018 - 12), SID 2018 - 14 reference 3, SID 2018 - 15 reference 2, Independence Lake Camp, and Logger LPO-060 
  
# Normalize the Paths

pipeline_files <- normalizePath(pipeline_files, winslash = "/", mustWork = TRUE)

# Create Catalog
ctg_test <- readLAScatalog(pipeline_files)

# Check Catalog
ctg_test

# Make a bounding box in the center of each tile for efficiency

df <- ctg_test@data
names(df)

box_size <- 500
half <- box_size / 2

qa_boxes <- data.frame(
  name = paste0("tile", seq_len(nrow(df))),   # tile1..tile8
  xmin = (df$Min.X + df$Max.X)/2 - half,
  xmax = (df$Min.X + df$Max.X)/2 + half,
  ymin = (df$Min.Y + df$Max.Y)/2 - half,
  ymax = (df$Min.Y + df$Max.Y)/2 + half
)


qa_boxes

# Create a bounding box to intersect tile
qa_polys <- st_sfc(
  lapply(seq_len(nrow(qa_boxes)), function(i) {
    st_polygon(list(matrix(c(
      qa_boxes$xmin[i], qa_boxes$ymin[i],
      qa_boxes$xmax[i], qa_boxes$ymin[i],
      qa_boxes$xmax[i], qa_boxes$ymax[i],
      qa_boxes$xmin[i], qa_boxes$ymax[i],
      qa_boxes$xmin[i], qa_boxes$ymin[i]
    ), ncol = 2, byrow = TRUE)))
  }),
  crs = st_crs(ctg_test)
)

qa_polys <- st_sf(name = qa_boxes$name, geometry = qa_polys)

# Intersect Tile to BB
ctg_qa <- catalog_intersect(ctg_test, qa_polys)
ctg_qa

sf::st_crs(ctg_test)
sf::st_crs(qa_polys)


plot(ctg_test)
plot(sf::st_geometry(qa_polys), add = TRUE, border = "red", lwd = 2)

```


## Filter Noise

When looking at the datasets there are some obvious levels of noise (in fact the amount of noise increased with each tile we examined). Filtering out these extraneous high and low (below the scene) points will help to reduce error and get us closer to the actual landscape. Note: this is only using distance to neighboring points/returns and no use of scan angle/intensity

First let's try the **ivf (isolated voxel function)** from lidR to filter the noise by searching the returns for ones that are far away from neighbors.Let's try using a filter where we filter points that are no more than 12 returns within a resolution of 4m. We then filter the points by removing those that are classified as noise (the LASNOISE = 7 and 18).

Overall, we we should check if the IVF being too strict by either counting how many points were removed. Commonly, \< 1-2% is fine but anything greater than 5% should be looked at visually. We will also visually check the filters to see if they were appropriate and we may play with parameters to see which produces the best result.

Additionally, we will be filtering the withheld and synthetic flagged points since they are not recommended for use as they could create bias in our data.


Here we will use the catalog to save space in R and RAM. 

```{r Generate Filter Pipeline - Center Boundary Box 500m}
# Set the number of threads to reduce RAM spikes
lidR::set_lidr_threads(1)

# Output folder
out_dir <- "Output_Files/Filter_Chunks/"
dir.create(out_dir, showWarnings = FALSE, recursive = TRUE)

# Sanity checks
stopifnot(length(pipeline_files) == nrow(qa_boxes))

for (k in seq_along(pipeline_files)) {

  f <- pipeline_files[k]
  base <- tools::file_path_sans_ext(basename(f))
  out_file <- file.path(out_dir, paste0(base, "_center500_filtered.laz"))

  message("\n[", k, "/", length(pipeline_files), "] ", base)

  # Skip if already done
  if (file.exists(out_file)) {
    message("  -> already exists, skipping: ", out_file)
    next
  }

  # Read full tile
  las <- readLAS(f)
  if (is.empty(las)) {
    message("  -> tile is empty, skipping")
    rm(las); gc()
    next
  }

  # Clip 500x500 center box for THIS tile
  clip <- clip_rectangle(
    las,
    xleft   = qa_boxes$xmin[k],
    ybottom = qa_boxes$ymin[k],
    xright  = qa_boxes$xmax[k],
    ytop    = qa_boxes$ymax[k]
  )

  # Free full tile ASAP
  rm(las); gc()

  if (is.empty(clip)) {
    message("  -> clip is empty, skipping")
    rm(clip); gc()
    next
  }

  # Filter pipeline on the clip
  clip <- filter_poi(clip, !Withheld_flag & !Synthetic_flag)
  clip <- classify_noise(clip, ivf(res = 4, n = 12))
  clip <- filter_poi(clip, !Classification %in% c(7L, 18L))

  if (is.empty(clip)) {
    message("  -> clip became empty after filtering, skipping")
    rm(clip); gc()
    next
  }

  # Write output
  writeLAS(clip, out_file)
  message("  -> wrote: ", out_file, " (n=", npoints(clip), ")")

  # Cleanup
  rm(clip); gc()
}


```

We will then check the chunks in CloudCompare to ensure there isn't any canopy shearing or noise left behind. 


## Ground Classification

Working in mountainous forests, we will need to keep track of the parameters that we use. It is standard to use a cloth simulation filter which is available through the LiDAR function csf(). With the csf function, the LiDAR point cloud is inverted, and a simulation of a “rigid cloth” is draped over the inverted point cloud. The simulated cloth surface is then used to determine which points are associated with the ground.

CSF is controlled by:

-   how stiff the 'cloth' is --\> rigidity

-   how coarse the 'cloth' mesh is --\> cloth resolution

-   much much sagging is allowed --\> slope smoothing


Cloth Resolution typical values: 

-   0.5 - 0.75 m = very fine cloth (captures small terrain detail)

-   1 m = medium cloth (balanced)

-   2–3 m = coarse cloth (very smooth terrain)

For our data set we have moderate-to-high density, so we could try 0.5m if 1m isn't working well. The smaller values (0.5m) typically is recommended for dense canopy, complex terrain, and reduces over-smoothing. However, it can eat a lot of RAM and run much slower.

Rigidness typical values:

-   1 = flexible cloth (follows small bumps)
-   2 = balanced (good starting point)
-   3 = very stiff (smooth terrain)

Class Threshold typical values: 

-   0.3 = stricter ground and may miss true ground under canopy
-   0.5 = good starting point and mid-range
-   0.7 - 1 = risk classifying low vegetation as ground

We will also be trying the PMF (progressive morphological filter) which creates a small window and finds the low points where it then gradually increases the window size and allow higher thresholds as terrain scale increases.Then, we will compare the two. I expect that the CSF may perform better compared to the PMF due to the mountainous terrain.

PMF is controlled by:

-   window size(s) (scale of terrain features)

-   height threshold (how much deviation from the ground is allowed)

**Here we will begin with starting with the default then check the DTM and tune from there. Since we are classifying these ourselves, we should be on the lookout for DTM pits, sinks, or 'wavy' terrain**

Window Size Typical Values:

-   3-5 = small to detect micro-terrain but may misclassify roots/rocks
-   5-10 = balanced (good default)
-   10+ = large for very smooth but can miss real terrain detail

Height Threshold Typical Values: 

-   0.5-1.0 = flat terrain
-   1.0-2.0 = forested terrain
-   1.5-3.0 = steep/mountainous 

Once points are classified, you know which points are associated with the ground, but because points are not continuous on the ground, you still need to interpolate between them to get an elevation map. For this, you can use a Delaunay triangulation which interpolates a continuous surface as a triangulation between the points classified as ground. You can implement the triangulation using the rasterize_terrain() function in the lidR package with the tin() algorithm selected.


```{r Generate Ground Classification Pipeline - Center Boundary Box 500m}


############# Set up Tiles (Do NOT run if you are running the previous chunk above and it is loaded into the environment) #############

# I had to close R so I need to find the filtered 500m centered ROI clips (so I do not need to re-run the filter)

filt_500_dir   <- "Output_Files/Filter_Chunks/"

# Grab only the ROI outputs that end with '_center500_filtered.laz'
filt_500_files <- list.files(filt_500_dir, pattern = "_center500_filtered\\.laz$", full.names = TRUE)

# Quick sanity check: how many files are there?

length(filt_500_files)
filt_500_files

dens <- sapply(filt_500_files, function(f) {
  h <- readLASheader(f)

  # Point count
  npts <- h@PHB[["Number of point records"]]

  # Bounding box area (m²)
  area <- (h@PHB[["Max X"]] - h@PHB[["Min X"]]) *
          (h@PHB[["Max Y"]] - h@PHB[["Min Y"]])

  npts / area
})

dens
summary(dens)

# Build a LAScatalog from those ROI clips 

ctg_filt_500 <- readLAScatalog(filt_500_files)

# Get a summary of the catalog
ctg_filt_500

######### Set up Run Information ##########

# We will set the run to process by file (due to the area being so small) - This means it will need to be altered for the larger pipeline

opt_chunk_size(ctg_filt_500)   <- 0 # set chunk size to 0
opt_chunk_buffer(ctg_filt_500) <- 0 # 0m buffer for the run

# Keep threads low for RAM stability
lidR::set_lidr_threads(1)

########### Create Output Folders for Each Method ##############

# Setting up output folders for each ground method allowing us to compare the two
dir.create(file.path(ctg_filt_500, "ground_csf"), showWarnings = FALSE, recursive = TRUE)
dir.create(file.path(ctg_filt_500, "ground_pmf"), showWarnings = FALSE, recursive = TRUE)

########## CSF Run ##########

# Define the algorithm settings 
## Cloth resolution - in m represents the 'grid size' of the cloth (smaller = more detailed, but slower)
## rigidness - how stiff the cloth is (higher = smouther surface)
## Class threshold - in m represents how far a point can be above the cloth and still be ground 
## Slope smooth - a setting that can help on slopes

csf_alg <- csf(
  sloop_smooth = TRUE, 
  class_threshold = 0.5, # 0.5m
  cloth_resolution = 0.5, # 0.75m --> not great changed to  0.5
  rigidness = 2
)

# Let lidR know how to name output files for the CSF run since we are not using chunks the {ORIGINALFILENAME} works
# Write ouptut LAZ files into roi_dir/ground_csf/

opt_output_files(ctg_filt_500) <- file.path(roi_dir, "ground_csf", "{ORIGINALFILENAME}_csf")

# Apply the CSF ground classification to each ROI clip and then return the LAS

catalog_apply(ctg_filt_500, function(cluster) {
  
  las <- readLAS(cluster)
  if(is.empty(las)) return(NULL)
  
  # Ground classification: sets classification == 2 for ground points 
  las <- classify_ground(las, csf_alg)
  
  # returning 'las' tells lidR to write it to the opt_output_files() path
  las
})

####### PMF Run ######

# Did not work well 

# Define the PMF algorithm 

# pmf_alg <- pmf(
#  ws = c(3, 5, 7, 9),     # progressive window sizes
#  th = c(0.5, 1, 1.5, 2)  # progressive thresholds
#)


# Create output names for the PMF

# opt_output_files(ctg_filt_500) <- file.path(roi_dir, "ground_pmf", "{ORIGINALFILENAME}_pmf")

# Apply PMF ground classification algorithm to each ROI clip

#catalog_apply(ctg_filt_500, function(cluster) {
  
#  las <- readLAS(cluster)
#  if (is.empty(las)) return(NULL)
  
#  las <- classify_ground(las, pmf_alg)
  
#  las
#})

```

CSF with the current parameters in the algorithm appear to be the best for our data. We can now the classification to make a DEM and a normalized canopy height model. 

## Generate DEM, Slope, and Aspect

Now that our point cloud has had the ground classified. We can create a Digital Elevation Model by interpolating the points which will help us as we make the normalized canopy height model.

** Note when making HSM it will likely be better to aggregate up to 5m or 10m for rasters. **

```{r Generate DEM, Slope, Aspect from Ground Classification - Center Boundary Box 500m}

############ Get the Files Prepped #########
# Path to your ground-classified files (CSF outputs)

csf_500_dir <- "Output_Files/Filter_Chunks/ground_csf/"

# List all .laz files inside that folder
# pattern = "\\.laz$" means: only files ending in .laz
# full.names = TRUE returns full file paths (required for catalog)

csf_500_files <- list.files(
  csf_500_dir,
  pattern = "\\.(las|laz)$",
  full.names = TRUE
)

stopifnot(length(csf_500_files) > 0)

# Create a LAScatalog object from those files
# A catalog does NOT load all points into RAM
# It only stores references to the files on disk

ctg_csf_500 <- readLAScatalog(csf_500_files)

# Print catalog info (extent, density, number of files)

ctg_csf_500

######## Set up lidR ########

# Process by file instead of spatial chunks so chunk_size = 0 means: treat each file as one unit

opt_chunk_size(ctg_csf_500) <- 0

# No buffer needed because

opt_chunk_buffer(ctg_csf_500) <- 0

# Limit threads to 1 to avoid RAM spikes

lidR::set_lidr_threads(1)

######## DEM ##########

# Create an output folder for the DTMs

dtm_dir <- "Output_Files/Filter_Chunks/dtm_csf_500_res1m/"
dir.create(dtm_dir, showWarnings = FALSE, recursive = TRUE)

# Define a function that lidR will apply to EACH file in the catalog

dtm_fun <- function(cluster, ...) {

  # Read the LAS data for this file (NOT the whole catalog)
  las <- readLAS(cluster)

  # If the file/chunk has no points, safely skip it
  # (prevents crashes in catalog_apply)
  if (is.empty(las)) return(NULL)

  # If no ground points exist, skip (prevents weird rasters)
  if (!any(las@data$Classification == 2L)) return(NULL)
  
  # Create a Digital Terrain Model (DTM)
  
    dtm <- rasterize_terrain(
    las, # Uses ONLY ground-classified points (class = 2)
    res = 1, # 1 meter grid resolution
    algorithm = tin() # tin() → Triangulated Irregular Network interpolation
  )

  # Return the raster so lidR can write it to disk
  return(dtm)
}

# Tell lidR how to name file

opt_output_files(ctg_csf_500) <- file.path(
  dtm_dir,
  "{ORIGINALFILENAME}_dtm"
)

# Apply the DTM function to every file in the catalog
catalog_apply(ctg_csf_500, dtm_fun)

###### Slope and Aspect ######

dtm_files <- list.files(dtm_dir, pattern="\\.(tif|grd)$", full.names=TRUE, ignore.case=TRUE)

slope_dir <- "Output_Files/Filter_Chunks/slope_res1m/"
asp_dir   <- "Output_Files/Filter_Chunks/aspect_res1m/"
dir.create(slope_dir, showWarnings = FALSE, recursive = TRUE)
dir.create(asp_dir,   showWarnings = FALSE, recursive = TRUE)

for (f in dtm_files) {
  dem <- rast(f)

  slope <- terrain(dem, v = "slope", unit = "degrees")
  aspect <- terrain(dem, v = "aspect", unit = "degrees")

  base <- tools::file_path_sans_ext(basename(f))
  writeRaster(slope,  file.path(slope_dir, paste0(base, "_slope.tif")), overwrite = TRUE)
  writeRaster(aspect, file.path(asp_dir,   paste0(base, "_aspect.tif")), overwrite = TRUE)
}
```

## Generate Normalized Canopy Height Model

Now that the ground is classified and we have the dem, we can make the normalized point cloud heights (height above ground). We can then make the canopy height model from these points (canopy top height per pixel)

```{r Generate Normalized Point Cloud and CHM - Center Boundary Box 500m}

########## Normalized Point Cloud ############
# Set up a directory to save files in 

norm_dir <- "Output_Files/Filter_Chunks/norm_csf/"
dir.create(norm_dir, showWarnings = FALSE, recursive = TRUE)

# Set up the lidR parameters

opt_chunk_size(ctg_csf_500) <- 0
opt_chunk_buffer(ctg_csf_500) <- 0
lidR::set_lidr_threads(1)

# Make a function normalize the point cloud

norm_fun <- function(cluster, ...) {
  las <- readLAS(cluster)
  if (is.empty(las)) return(NULL)
  if (!any(las@data$Classification == 2L)) return(NULL)  # must have ground

  las <- normalize_height(las, tin())
  las
}

# Tell lidR where to save and how to name files

opt_output_files(ctg_csf_500) <- file.path(norm_dir, "{ORIGINALFILENAME}_norm")

# Apply the normalization function to the catalog

catalog_apply(ctg_csf_500, norm_fun)

########### Build a Canopy Height Model ###########

# Create a directory to save the files

chm_dir <- "Output_Files/Filter_Chunks/chm_csf_res1m/"
dir.create(chm_dir, showWarnings = FALSE, recursive = TRUE)

# Create a catalog of the normalized point cloud

ctg_500_norm <- readLAScatalog(list.files(norm_dir, pattern="\\.(las|laz)$", full.names=TRUE, ignore.case=TRUE))

# Set up lidR parameters
opt_chunk_size(ctg_500_norm) <- 0
opt_chunk_buffer(ctg_500_norm) <- 0
lidR::set_lidr_threads(1)

# Create a function for the Canopy Height Model

chm_fun <- function(cluster, ...) {
  las <- readLAS(cluster)
  if (is.empty(las)) return(NULL)

  # Canopy height model: typically use max height per cell
  chm <- rasterize_canopy(
  las,
  res = 1,
  algorithm = pitfree(c(0,2,5,10,15,20,25), subcircle = 0.2)
)

  chm
}

opt_output_files(ctg_500_norm) <- file.path(chm_dir, "{ORIGINALFILENAME}_chm")
catalog_apply(ctg_500_norm, chm_fun)
```

## Understory Density and Complexity

```{r Generate Understory and Complexity - Center Boundary Box 500m}

###### Understory Metrics ##########
# Folder containing normalized point clouds
norm_dir <- "Output_Files/Filter_Chunks/norm_csf/"

# List ALL normalized LAS/LAZ files
norm_files <- list.files(
  norm_dir,
  pattern = "\\.(las|laz)$",  # important: you have .las files, not .laz
  full.names = TRUE
)

# Check what was found (very important QA step)
norm_files

# Create the catalog (this does NOT load points into RAM)
ctg_500_norm <- readLAScatalog(norm_files)

# Inspect catalog
ctg_500_norm

# Create Understory Directory
under_dir <- "Output_Files/Filter_Chunks/understory_res10m/"
dir.create(under_dir, showWarnings = FALSE, recursive = TRUE)

opt_chunk_size(ctg_500_norm) <- 0
opt_chunk_buffer(ctg_500_norm) <- 0
lidR::set_lidr_threads(1)

res <- 10
cell_area <- res * res

files <- ctg_500_norm@data$filename

for (f in files) {
  las <- readLAS(f)
  if (is.empty(las)) {
    message("SKIP empty file: ", basename(f))
    next
  }

  base <- tools::file_path_sans_ext(basename(f))
  message("Processing: ", base, " | npoints=", npoints(las))

  # total count per cell -> density
  total_count <- pixel_metrics(las, ~length(Z), res = res)
  if (ncell(total_count) == 0 || all(is.na(values(total_count)))) {
    message("  SKIP: total_count empty/NA for ", base)
    next
  }
  total_density <- total_count / cell_area

  # understory points
  under_pts <- filter_poi(las, Classification != 2L & Z < 2)
  if (is.empty(under_pts)) {
    under_density <- total_density; values(under_density) <- 0
    pct_under <- total_density; values(pct_under) <- 0
  } else {
    under_count <- pixel_metrics(under_pts, ~length(Z), res = res)

    # align grids if needed
    if (!compareGeom(total_count, under_count, stopOnError = FALSE)) {
      under_count <- resample(under_count, total_count, method = "near")
    }

    under_density <- under_count / cell_area
    pct_under <- under_density / total_density
    pct_under[!is.finite(pct_under)] <- NA
  }

  writeRaster(total_density,
              file.path(under_dir, paste0(base, "_dens_total_", res, "m.tif")),
              overwrite = TRUE)
  writeRaster(under_density,
              file.path(under_dir, paste0(base, "_dens_under_0to2m_", res, "m.tif")),
              overwrite = TRUE)
  writeRaster(pct_under,
              file.path(under_dir, paste0(base, "_pct_under_0to2m_", res, "m.tif")),
              overwrite = TRUE)

  message("  WROTE rasters for: ", base)
}


###### Complexity #######

complex_dir <- "Output_Files/Filter_Chunks/complexity_res1m/"
dir.create(complex_dir, showWarnings = FALSE, recursive = TRUE)

complex_fun <- function(cluster, ...) {
  las <- readLAS(cluster)
  if (is.empty(las)) return(NULL)

  # Use normalized heights; remove ground if you want canopy-only complexity:
  las_can <- filter_poi(las, Classification != 2L & Z > 0)

  # coefficient of variation of heights in each 1m pixel
  comp <- pixel_metrics(las_can, ~ sd(Z) / mean(Z), res = 1)

  comp
}

opt_output_files(ctg_500_norm) <- file.path(complex_dir, "{ORIGINALFILENAME}_complexity")
catalog_apply(ctg_500_norm, complex_fun)

### If you prefer complexity that includes all points (including ground), remove the filter_poi() line — but for vegetation structure metrics, canopy-only is usually more meaningful.
```


Looking at QGIS I noticed some descrepancies where some tiles appear in random locations well outside the study area.

# Bug Fix

```{r Check QGIS Descrepancy}
########## PRESERVE ORIGINAL FILE LIST (DO NOT OVERWRITE) ##########

roi_filtered_directory_path <- "Output_Files/Filter_Chunks/"

# Identify ONLY the 500m ROI clips created earlier in the pipeline
roi_filtered_clip_file_paths <- list.files(
  path = roi_filtered_directory_path,
  pattern = "_center500_filtered\\.laz$",  # strict match to your naming scheme
  full.names = TRUE
)

# Sanity check: ensure files were found
stopifnot(length(roi_filtered_clip_file_paths) > 0)

########## FUNCTION: EXTRACT CRS WKT FROM LAS HEADER (MEMORY SAFE) ##########
# This reads ONLY metadata, not the full point cloud

extract_crs_wkt_from_las_header <- function(single_laz_file_path) {
  
  # Read LAS header only (very low RAM usage)
  las_header_object <- lidR::readLASheader(single_laz_file_path)
  
  # Extract CRS as WKT (most reliable identifier across flights)
  crs_wkt_value <- sf::st_crs(las_header_object)$wkt
  
  # Return NA if CRS is missing
  if (is.null(crs_wkt_value) || is.na(crs_wkt_value)) {
    return(NA_character_)
  }
  
  return(crs_wkt_value)
}

########## BUILD DETAILED CRS AUDIT TABLE ##########

roi_clip_crs_audit_table <- data.table(
  roi_clip_file_path = roi_filtered_clip_file_paths
)

# Extract CRS WKT for each ROI clip (no point cloud loading)
roi_clip_crs_audit_table[
  , crs_wkt := vapply(
    roi_clip_file_path,
    extract_crs_wkt_from_las_header,
    character(1)
  )
]

########## SUMMARIZE DISTINCT CRS GROUPS ##########
# This will reveal if LAZ + SouthernIdaho2018_14_LAZ are in a different CRS

roi_clip_crs_summary_table <- roi_clip_crs_audit_table[
  , .(number_of_files = .N),
  by = crs_wkt
][order(-number_of_files)]

# Print clear diagnostic output
roi_clip_crs_summary_table

########## INPUT: ROI CLIPS CREATED BY YOUR FILTER STEP ##########

roi_filtered_directory_path <- "Output_Files/Filter_Chunks/"

roi_filtered_clip_file_paths <- list.files(
  path = roi_filtered_directory_path,
  pattern = "_center500_filtered\\.laz$",
  full.names = TRUE
)

stopifnot(length(roi_filtered_clip_file_paths) > 0)

########## FUNCTION: EXTRACT HEADER CRS WKT (MEMORY SAFE) ##########

extract_crs_wkt_from_las_header <- function(single_laz_file_path) {
  las_header_object <- lidR::readLASheader(single_laz_file_path)
  crs_wkt_value <- sf::st_crs(las_header_object)$wkt
  if (is.null(crs_wkt_value) || is.na(crs_wkt_value)) return(NA_character_)
  crs_wkt_value
}

########## BUILD AUDIT TABLE ##########

roi_clip_crs_audit_table <- data.table(
  roi_clip_file_path = roi_filtered_clip_file_paths
)

roi_clip_crs_audit_table[
  , crs_wkt := vapply(
    roi_clip_file_path,
    extract_crs_wkt_from_las_header,
    character(1)
  )
]

# Parse UTM zone from the WKT text (works for your three CRS strings)
roi_clip_crs_audit_table[
  , utm_zone := fifelse(
    str_detect(crs_wkt, "UTM zone 11N"), "11N",
    fifelse(str_detect(crs_wkt, "UTM zone 12N"), "12N", NA_character_)
  )
]

# Parse geoid model from WKT (Geoid12B vs Geoid18)
roi_clip_crs_audit_table[
  , geoid_model := fifelse(
    str_detect(crs_wkt, "GEOID12B"), "GEOID12B",
    fifelse(str_detect(crs_wkt, "GEOID18"), "GEOID18", NA_character_)
  )
]

# Helpful filename column for reading output
roi_clip_crs_audit_table[
  , roi_clip_base_name := tools::file_path_sans_ext(basename(roi_clip_file_path))
]

roi_clip_crs_audit_table[, .(roi_clip_base_name, utm_zone, geoid_model)]

########## DEFINE SEPARATE OUTPUT ROOTS (VERY IMPORTANT) ##########

output_root_directory <- "Output_Files/Filter_Chunks/"

ground_csf_utm11_dir <- file.path(output_root_directory, "ground_csf_utm11")
ground_csf_utm12_dir <- file.path(output_root_directory, "ground_csf_utm12")

dir.create(ground_csf_utm11_dir, showWarnings = FALSE, recursive = TRUE)
dir.create(ground_csf_utm12_dir, showWarnings = FALSE, recursive = TRUE)
```

We will need to rerun this. 

```{r Fix Bugs}

#### 1) Inputs were created - verify they exist and make a file path   ####

# Assumes these already exist in your environment from your audit step:
# - roi_clip_crs_audit_table with columns:
#     roi_clip_file_path, crs_wkt, utm_zone, geoid_model, roi_clip_base_name
# - output_root_directory <- "Output_Files/Filter_Chunks/"
# - ground_csf_utm11_dir, ground_csf_utm12_dir already created

stopifnot(exists("roi_clip_crs_audit_table"))
stopifnot(exists("output_root_directory"))
stopifnot(exists("ground_csf_utm11_dir"))
stopifnot(exists("ground_csf_utm12_dir"))

stopifnot(dir.exists(output_root_directory))
stopifnot(dir.exists(ground_csf_utm11_dir))
stopifnot(dir.exists(ground_csf_utm12_dir))

# Extract ROI clip paths by UTM zone (these are your filtered 500m ROI files)
roi_filtered_clip_paths_utm11 <- roi_clip_crs_audit_table[utm_zone == "11N", roi_clip_file_path]
roi_filtered_clip_paths_utm12 <- roi_clip_crs_audit_table[utm_zone == "12N", roi_clip_file_path]

stopifnot(length(roi_filtered_clip_paths_utm11) > 0)
stopifnot(length(roi_filtered_clip_paths_utm12) > 0)

# A single folder per CRS that contains everything.

metrics_utm11_root_dir <- file.path(output_root_directory, "metrics_utm11")
metrics_utm12_root_dir <- file.path(output_root_directory, "metrics_utm12")

dir.create(metrics_utm11_root_dir, showWarnings = FALSE, recursive = TRUE)
dir.create(metrics_utm12_root_dir, showWarnings = FALSE, recursive = TRUE)

# Within each parent, create standardized subfolders
create_metrics_subfolders <- function(metrics_root_dir) {

  vertical_root_dir <- file.path(metrics_root_dir, "vertical_bins_res5m")

  subfolder_list <- list(
    ground_csf        = file.path(metrics_root_dir, "ground_csf"),
    dtm_res1m         = file.path(metrics_root_dir, "dtm_res1m"),
    slope_res1m       = file.path(metrics_root_dir, "slope_res1m"),
    aspect_res1m      = file.path(metrics_root_dir, "aspect_res1m"),
    norm_csf          = file.path(metrics_root_dir, "norm_csf"),
    chm_res1m         = file.path(metrics_root_dir, "chm_res1m"),
    complexity_res1m  = file.path(metrics_root_dir, "complexity_res1m"),

    # NEW: Vertical bins with folders per bin + a summary folder
    vertical_bins_5m = list(
      root       = vertical_root_dir,
      summary    = file.path(vertical_root_dir, "summary"),
      bin_0to2m   = file.path(vertical_root_dir, "bin_0to2m"),
      bin_2to5m   = file.path(vertical_root_dir, "bin_2to5m"),
      bin_5to10m  = file.path(vertical_root_dir, "bin_5to10m"),
      bin_10mplus = file.path(vertical_root_dir, "bin_10mplus")
    ),

    # NEW: Rugosity outputs
    rugosity_5m = file.path(metrics_root_dir, "rugosity_res5m")
  )

  # Create simple folders
  for (nm in names(subfolder_list)) {
    if (nm == "vertical_bins_5m") next
    dir.create(subfolder_list[[nm]], showWarnings = FALSE, recursive = TRUE)
  }

  # Create vertical bin folders
  for (single_dir in subfolder_list$vertical_bins_5m) {
    dir.create(single_dir, showWarnings = FALSE, recursive = TRUE)
  }

  return(subfolder_list)
}

utm11_dirs <- create_metrics_subfolders(metrics_utm11_root_dir)
utm12_dirs <- create_metrics_subfolders(metrics_utm12_root_dir)

# Print for QA so you can SEE where stuff will go
print(utm11_dirs)
print(utm12_dirs)



#### 2) Build CRS - Clean ROI Catalogs      ####

# These catalogs point to the already-filtered ROI clips on disk.
# They do NOT create any new files by themselves.

ctg_roi_filtered_utm11 <- lidR::readLAScatalog(roi_filtered_clip_paths_utm11)
ctg_roi_filtered_utm12 <- lidR::readLAScatalog(roi_filtered_clip_paths_utm12)

# Process by file: chunk_size=0 means each file is processed independently
lidR::opt_chunk_size(ctg_roi_filtered_utm11) <- 0
lidR::opt_chunk_buffer(ctg_roi_filtered_utm11) <- 0

lidR::opt_chunk_size(ctg_roi_filtered_utm12) <- 0
lidR::opt_chunk_buffer(ctg_roi_filtered_utm12) <- 0

lidR::set_lidr_threads(1)

# Optional: print summaries for QA
ctg_roi_filtered_utm11
ctg_roi_filtered_utm12

#### 3) CSF Ground Classification (BOTH) ####

# This step creates NEW files on disk:
# - Output_Files/Filter_Chunks/ground_csf_utm11/*_csf.laz
# - Output_Files/Filter_Chunks/ground_csf_utm12/*_csf.laz

csf_algorithm_object <- lidR::csf(
  sloop_smooth = TRUE,
  class_threshold = 0.5,
  cloth_resolution = 0.5,
  rigidness = 2
)

########## CSF RUN: UTM 11 ##########

lidR::opt_output_files(ctg_roi_filtered_utm11) <- file.path(
  utm11_dirs$ground_csf,
  "{ORIGINALFILENAME}_csf.laz"
)

lidR::catalog_apply(ctg_roi_filtered_utm11, function(cluster) {

  las_object <- lidR::readLAS(cluster)
  if (lidR::is.empty(las_object)) return(NULL)

  las_object <- lidR::classify_ground(las_object, csf_algorithm_object)

  las_object
})

########## CSF RUN: UTM 12 ##########

lidR::opt_output_files(ctg_roi_filtered_utm12) <- file.path(
  utm12_dirs$ground_csf,
  "{ORIGINALFILENAME}_csf.laz"
)

lidR::catalog_apply(ctg_roi_filtered_utm12, function(cluster) {

  las_object <- lidR::readLAS(cluster)
  if (lidR::is.empty(las_object)) return(NULL)

  las_object <- lidR::classify_ground(las_object, csf_algorithm_object)

  las_object
})


########## FIX: DO NOT INCLUDE .las/.laz EXTENSION ##########

lidR::opt_output_files(ctg_roi_filtered_utm11) <- file.path(
  utm11_dirs$ground_csf,
  "{ORIGINALFILENAME}_csf"
)

lidR::opt_output_files(ctg_roi_filtered_utm12) <- file.path(
  utm12_dirs$ground_csf,
  "{ORIGINALFILENAME}_csf"
)

########## FIX: MATCH ANY LAS/LAZ EXTENSION ##########

csf_output_paths_utm11 <- list.files(
  utm11_dirs$ground_csf,
  pattern = "_csf\\.(las|laz)$",
  full.names = TRUE,
  ignore.case = TRUE
)

csf_output_paths_utm12 <- list.files(
  utm12_dirs$ground_csf,
  pattern = "_csf\\.(las|laz)$",
  full.names = TRUE,
  ignore.case = TRUE
)

# If your accidental files are still ".laz.las", match them too:
if (length(csf_output_paths_utm11) == 0) {
  csf_output_paths_utm11 <- list.files(
    utm11_dirs$ground_csf,
    pattern = "_csf\\.laz\\.las$",
    full.names = TRUE,
    ignore.case = TRUE
  )
}

if (length(csf_output_paths_utm12) == 0) {
  csf_output_paths_utm12 <- list.files(
    utm12_dirs$ground_csf,
    pattern = "_csf\\.laz\\.las$",
    full.names = TRUE,
    ignore.case = TRUE
  )
}

stopifnot(length(csf_output_paths_utm11) > 0)
stopifnot(length(csf_output_paths_utm12) > 0)

########## CLEANUP: RENAME *.laz.las -> *.las ##########

rename_laz_las_double_extension <- function(folder_path) {

  double_extension_file_paths <- list.files(
    folder_path,
    pattern = "\\.laz\\.las$",
    full.names = TRUE,
    ignore.case = TRUE
  )

  if (length(double_extension_file_paths) == 0) return(invisible(NULL))

  for (old_path in double_extension_file_paths) {

    new_path <- sub("\\.laz\\.las$", ".las", old_path, ignore.case = TRUE)

    # Do not overwrite anything accidentally
    if (file.exists(new_path)) next

    file.rename(from = old_path, to = new_path)
  }
}

rename_laz_las_double_extension(utm11_dirs$ground_csf)
rename_laz_las_double_extension(utm12_dirs$ground_csf)

csf_output_paths_utm11 <- list.files(utm11_dirs$ground_csf, pattern = "_csf\\.(las|laz)$", full.names = TRUE, ignore.case = TRUE)
csf_output_paths_utm12 <- list.files(utm12_dirs$ground_csf, pattern = "_csf\\.(las|laz)$", full.names = TRUE, ignore.case = TRUE)

# Check 
list.files(utm11_dirs$ground_csf)

#### 4) Build Catalogs from CSF        ####


csf_output_paths_utm11 <- list.files(
  utm11_dirs$ground_csf,
  pattern = "_csf\\.(las|laz)$",
  full.names = TRUE,
  ignore.case = TRUE
)

csf_output_paths_utm12 <- list.files(
  utm12_dirs$ground_csf,
  pattern = "_csf\\.(las|laz)$",
  full.names = TRUE,
  ignore.case = TRUE
)

stopifnot(length(csf_output_paths_utm11) > 0)
stopifnot(length(csf_output_paths_utm12) > 0)

ctg_csf_utm11 <- lidR::readLAScatalog(csf_output_paths_utm11)
ctg_csf_utm12 <- lidR::readLAScatalog(csf_output_paths_utm12)

lidR::opt_chunk_size(ctg_csf_utm11) <- 0
lidR::opt_chunk_buffer(ctg_csf_utm11) <- 0

lidR::opt_chunk_size(ctg_csf_utm12) <- 0
lidR::opt_chunk_buffer(ctg_csf_utm12) <- 0

lidR::set_lidr_threads(1)

#### 5) DTM (1m) for UTM11 and UTM12      ####
# Outputs:
# - Output_Files/Filter_Chunks/dtm_res1m_utm11/*_dtm.tif
# - Output_Files/Filter_Chunks/dtm_res1m_utm12/*_dtm.tif

dtm_fun <- function(cluster, ...) {

  las_object <- lidR::readLAS(cluster)
  if (lidR::is.empty(las_object)) return(NULL)

  # Skip if there are no ground points
  if (!any(las_object@data$Classification == 2L)) return(NULL)

  dtm_raster <- lidR::rasterize_terrain(
    las_object,
    res = 1,
    algorithm = lidR::tin()
  )

  return(dtm_raster)
}

########## DTM: UTM11 ##########

lidR::opt_output_files(ctg_csf_utm11) <- file.path(
  utm11_dirs$dtm_res1m,
  "{ORIGINALFILENAME}_dtm"
)

lidR::catalog_apply(ctg_csf_utm11, dtm_fun)

########## DTM: UTM12 ##########

lidR::opt_output_files(ctg_csf_utm12) <- file.path(
  utm12_dirs$dtm_res1m,
  "{ORIGINALFILENAME}_dtm"
)

lidR::catalog_apply(ctg_csf_utm12, dtm_fun)

#### 6) Slope + Aspect from DTM (BOTH)    ####

# Outputs:
# - slope_res1m_utm11/*_slope.tif
# - aspect_res1m_utm11/*_aspect.tif
# - slope_res1m_utm12/*_slope.tif
# - aspect_res1m_utm12/*_aspect.tif

dtm_paths_utm11 <- list.files(
  utm11_dirs$dtm_res1m,
  pattern = "_dtm\\.(tif|grd)$",
  full.names = TRUE,
  ignore.case = TRUE
)

dtm_paths_utm12 <- list.files(
  utm12_dirs$dtm_res1m,
  pattern = "_dtm\\.(tif|grd)$",
  full.names = TRUE,
  ignore.case = TRUE
)

stopifnot(length(dtm_paths_utm11) > 0)
stopifnot(length(dtm_paths_utm12) > 0)

########## SLOPE/ASPECT: UTM11 ##########

for (single_dtm_path in dtm_paths_utm11) {

  dtm_rast <- terra::rast(single_dtm_path)

  slope_rast  <- terra::terrain(dtm_rast, v = "slope", unit = "degrees")
  aspect_rast <- terra::terrain(dtm_rast, v = "aspect", unit = "degrees")

  base_name <- tools::file_path_sans_ext(basename(single_dtm_path))

  terra::writeRaster(
    slope_rast,
    filename = file.path(utm11_dirs$slope_res1m, paste0(base_name, "_slope.tif")),
    overwrite = TRUE
  )

  terra::writeRaster(
    aspect_rast,
    filename = file.path(utm11_dirs$aspect_res1m, paste0(base_name, "_aspect.tif")),
    overwrite = TRUE
  )
}

########## SLOPE/ASPECT: UTM12 ##########

for (single_dtm_path in dtm_paths_utm12) {

  dtm_rast <- terra::rast(single_dtm_path)

  slope_rast  <- terra::terrain(dtm_rast, v = "slope", unit = "degrees")
  aspect_rast <- terra::terrain(dtm_rast, v = "aspect", unit = "degrees")

  base_name <- tools::file_path_sans_ext(basename(single_dtm_path))

  terra::writeRaster(
    slope_rast,
    filename = file.path(utm12_dirs$slope_res1m, paste0(base_name, "_slope.tif")),
    overwrite = TRUE
  )

  terra::writeRaster(
    aspect_rast,
    filename = file.path(utm12_dirs$aspect_res1m, paste0(base_name, "_aspect.tif")),
    overwrite = TRUE
  )
}

#### 7) Normalize Height (BOTH)            ####

# Outputs:
# - norm_csf_utm11/*_norm.laz
# - norm_csf_utm12/*_norm.laz

norm_fun <- function(cluster, ...) {

  las_object <- lidR::readLAS(cluster)
  if (lidR::is.empty(las_object)) return(NULL)

  if (!any(las_object@data$Classification == 2L)) return(NULL)

  normalized_las_object <- lidR::normalize_height(las_object, lidR::tin())

  return(normalized_las_object)
}

########## NORMALIZE: UTM11 ##########

lidR::opt_output_files(ctg_csf_utm11) <- file.path(
  utm11_dirs$norm_csf,
  "{ORIGINALFILENAME}_norm"
)

lidR::catalog_apply(ctg_csf_utm11, norm_fun)

########## NORMALIZE: UTM12 ##########

lidR::opt_output_files(ctg_csf_utm12) <- file.path(
  utm12_dirs$norm_csf,
  "{ORIGINALFILENAME}_norm"
)

lidR::catalog_apply(ctg_csf_utm12, norm_fun)

#### 8) Build Normalized Catalog (BOTH)             ####

norm_paths_utm11 <- list.files(
  utm11_dirs$norm_csf,
  pattern = "_norm\\.(las|laz)$",
  full.names = TRUE,
  ignore.case = TRUE
)

norm_paths_utm12 <- list.files(
  utm12_dirs$norm_csf,
  pattern = "_norm\\.(las|laz)$",
  full.names = TRUE,
  ignore.case = TRUE
)

stopifnot(length(norm_paths_utm11) > 0)
stopifnot(length(norm_paths_utm12) > 0)

ctg_norm_utm11 <- lidR::readLAScatalog(norm_paths_utm11)
ctg_norm_utm12 <- lidR::readLAScatalog(norm_paths_utm12)

lidR::opt_chunk_size(ctg_norm_utm11) <- 0
lidR::opt_chunk_buffer(ctg_norm_utm11) <- 0

lidR::opt_chunk_size(ctg_norm_utm12) <- 0
lidR::opt_chunk_buffer(ctg_norm_utm12) <- 0

lidR::set_lidr_threads(1)

#### 9) CHM (1m) (BOTH)                      ####

# Outputs:
# - chm_res1m_utm11/*_chm.tif
# - chm_res1m_utm12/*_chm.tif

chm_fun <- function(cluster, ...) {

  las_object <- lidR::readLAS(cluster)
  if (lidR::is.empty(las_object)) return(NULL)

  chm_raster <- lidR::rasterize_canopy(
    las_object,
    res = 1,
    algorithm = lidR::pitfree(c(0,2,5,10,15,20,25), subcircle = 0.2)
  )

  return(chm_raster)
}

########## CHM: UTM11 ##########

lidR::opt_output_files(ctg_norm_utm11) <- file.path(
  utm11_dirs$chm_res1m,
  "{ORIGINALFILENAME}_chm"
)

lidR::catalog_apply(ctg_norm_utm11, chm_fun)

########## CHM: UTM12 ##########

lidR::opt_output_files(ctg_norm_utm12) <- file.path(
  utm12_dirs$chm_res1m,
  "{ORIGINALFILENAME}_chm"
)

lidR::catalog_apply(ctg_norm_utm12, chm_fun)

###### Step 10 ######
###### Vertical bins (5m) + Canopy cover (first returns only; canopy >= 10m) ######

# PURPOSE:
# - Compute vegetation-only vertical structure metrics in 5x5m cells
# - Write outputs into per-bin folders + a summary folder
#
# IMPORTANT LOGIC:
# - If there is NO vegetation in a pixel: density/percent/cover = 0 (NOT NA)
# - Height stats (mean/sd/p95) are NA when there are no points in that bin
# - Percent-in-bin denominator = vegetation points ONLY (not all points)

####### Step 10 PREP (run once, OUTSIDE any loop) #######

# Extracts a tile key like 11TQG380725 or 12TTM790770 from the filename. Found file names got too long for Windows 
# Falls back to a shortened basename if the pattern isn't found.
extract_tile_key <- function(single_norm_path) {
  file_stem <- tools::file_path_sans_ext(basename(single_norm_path))

  # Pattern: 2 digits + 3 letters + 6 digits (e.g., 11TQG380725)
  m <- regexpr("\\b\\d{2}[A-Z]{3}\\d{6}\\b", file_stem, perl = TRUE)

  if (m[1] != -1) {
    return(regmatches(file_stem, m)[1])
  }

  # Fallback: strip very long standardized suffix if present
  file_stem <- sub("_center500_filtered_.*$", "", file_stem)

  # If still long, hard truncate as last resort
  substr(file_stem, 1, 60)
}

# Resolution for vertical bin rasters
vertical_res_m <- 5
vertical_cell_area_m2 <- vertical_res_m * vertical_res_m

# Height bins: [low, high)
height_bin_definitions <- list(
  bin_0to2m   = c(0, 2),
  bin_2to5m   = c(2, 5),
  bin_5to10m  = c(5, 10),
  bin_10mplus = c(10, Inf)
)

# "True canopy cover" threshold (for canopy ~15–25m typical, >=10m is reasonable)
canopy_height_threshold_m <- 10

# Force terra tempdir to a known writable folder (fixes many writeRaster failures)
terra_tmp <- file.path(output_root_directory, "terra_tmp")
dir.create(terra_tmp, showWarnings = FALSE, recursive = TRUE)
terra::terraOptions(tempdir = terra_tmp)
stopifnot(dir.exists(terra_tmp))
stopifnot(file.access(terra_tmp, 2) == 0)  # 0 means writable

# Safe p95 helper
p95_fun <- function(z) {
  if (length(z) == 0) return(NA_real_)
  as.numeric(stats::quantile(z, probs = 0.95, na.rm = TRUE, type = 7))
}

# WriteRaster wrapper that fails with the exact filename (so debugging is painless)
safe_write <- function(r, filename) {
  tryCatch(
    terra::writeRaster(r, filename = filename, overwrite = TRUE),
    error = function(e) stop("writeRaster failed for:\n  ", filename, "\nReason: ", conditionMessage(e))
  )
}

####### Step 10 FUNCTION #######

compute_and_write_vertical_bins <- function(single_norm_path, vertical_bins_dir_list) {

  ###### 10.1 Load normalized LAS ######
  las_object <- lidR::readLAS(single_norm_path)
  if (lidR::is.empty(las_object)) return(invisible(NULL))

 # Use tile key for output prefixes (short + meaningful)
  tile_key <- extract_tile_key(single_norm_path)
  message("Step 10 (veg-only bins): ", tile_key)

  # Optional but helpful: absolute, normalized output dirs (removes //)
  summary_dir <- normalizePath(vertical_bins_dir_list$summary, winslash = "\\", mustWork = TRUE)

  ###### 10.2 Template grid (geometry only) ######
  # Using all points ONLY to create the raster grid (extent/resolution)
  template_count <- lidR::pixel_metrics(las_object, ~length(Z), res = vertical_res_m)
  if (terra::ncell(template_count) == 0) {
    message("  SKIP: template grid empty for ", tile_key)
    return(invisible(NULL))
  }

  ###### 10.3 Define vegetation-only points (your metrics are ONLY based on veg) ######
  veg_all_pts <- lidR::filter_poi(las_object, Classification != 2L & Z > 0)

  ###### 10.4 Vegetation count raster (denominator) ######
  # If there is no vegetation anywhere: veg_count is 0 everywhere (NOT NA)
  if (lidR::is.empty(veg_all_pts)) {
    veg_count_rast <- template_count
    terra::values(veg_count_rast) <- 0
  } else {
    veg_count_rast <- lidR::pixel_metrics(veg_all_pts, ~length(Z), res = vertical_res_m)

    # Rare mismatch insurance
    if (!terra::compareGeom(template_count, veg_count_rast, stopOnError = FALSE)) {
      veg_count_rast <- terra::resample(veg_count_rast, template_count, method = "near")
    }
  }

  safe_write(
    veg_count_rast,
    file.path(vertical_bins_dir_list$summary, paste0(tile_key, "_vegcount_5m.tif"))
  )

  ###### 10.5 Total vegetation density (points per m²) ######
  veg_density_rast <- veg_count_rast / vertical_cell_area_m2

  safe_write(
    veg_density_rast,
    file.path(vertical_bins_dir_list$summary, paste0(tile_key, "_dens_veg_total_5m.tif"))
  )

  ###### 10.6 Canopy cover (FIRST RETURNS ONLY; veg-only; >= threshold) ######
  # cover = (# first-return veg points with Z >= threshold) / (# first-return veg points)
  # if denom=0 => cover=0

  las_cover <- las_object

  if ("ReturnNumber" %in% names(las_cover@data)) {
    las_cover <- lidR::filter_poi(las_cover, ReturnNumber == 1L)
  }

  las_cover <- lidR::filter_poi(las_cover, Classification != 2L & Z > 0)

  if (lidR::is.empty(las_cover)) {
    canopy_cover_rast <- template_count
    terra::values(canopy_cover_rast) <- 0
  } else {
    canopy_cover_rast <- lidR::pixel_metrics(
      las_cover,
      ~ ifelse(length(Z) == 0, 0, sum(Z >= canopy_height_threshold_m) / length(Z)),
      res = vertical_res_m
    )

    if (!terra::compareGeom(template_count, canopy_cover_rast, stopOnError = FALSE)) {
      canopy_cover_rast <- terra::resample(canopy_cover_rast, template_count, method = "near")
    }
  }

  safe_write(
    canopy_cover_rast,
    file.path(
      vertical_bins_dir_list$summary,
      paste0(tile_key, "_canopycover_firstreturn_veg_ge", canopy_height_threshold_m, "m_5m.tif")
    )
  )

  ###### 10.7 Per-bin rasters ######
  for (bin_name in names(height_bin_definitions)) {

    zlo <- height_bin_definitions[[bin_name]][1]
    zhi <- height_bin_definitions[[bin_name]][2]

    # Bin points: veg-only + stratum filter
    bin_pts <- lidR::filter_poi(veg_all_pts, Z >= zlo & Z < zhi)

    out_dir <- switch(
      bin_name,
      bin_0to2m   = vertical_bins_dir_list$bin_0to2m,
      bin_2to5m   = vertical_bins_dir_list$bin_2to5m,
      bin_5to10m  = vertical_bins_dir_list$bin_5to10m,
      bin_10mplus = vertical_bins_dir_list$bin_10mplus
    )

    # Initialize rasters from template
    bin_count_rast <- template_count
    bin_mean_rast  <- template_count
    bin_sd_rast    <- template_count
    bin_p95_rast   <- template_count

    # Defaults: no points => density/percent=0, stats=NA
    terra::values(bin_count_rast) <- 0
    terra::values(bin_mean_rast)  <- NA_real_
    terra::values(bin_sd_rast)    <- NA_real_
    terra::values(bin_p95_rast)   <- NA_real_

    if (!lidR::is.empty(bin_pts)) {

      # Counts
      bin_count_tmp <- lidR::pixel_metrics(bin_pts, ~length(Z), res = vertical_res_m)
      if (!terra::compareGeom(template_count, bin_count_tmp, stopOnError = FALSE)) {
        bin_count_tmp <- terra::resample(bin_count_tmp, template_count, method = "near")
      }
      terra::values(bin_count_rast) <- terra::values(bin_count_tmp)

      # Stats
      bin_mean_tmp <- lidR::pixel_metrics(bin_pts, ~mean(Z),   res = vertical_res_m)
      bin_sd_tmp   <- lidR::pixel_metrics(bin_pts, ~sd(Z),     res = vertical_res_m)
      bin_p95_tmp  <- lidR::pixel_metrics(bin_pts, ~p95_fun(Z),res = vertical_res_m)

      if (!terra::compareGeom(template_count, bin_mean_tmp, stopOnError = FALSE)) {
        bin_mean_tmp <- terra::resample(bin_mean_tmp, template_count, method = "near")
      }
      if (!terra::compareGeom(template_count, bin_sd_tmp, stopOnError = FALSE)) {
        bin_sd_tmp <- terra::resample(bin_sd_tmp, template_count, method = "near")
      }
      if (!terra::compareGeom(template_count, bin_p95_tmp, stopOnError = FALSE)) {
        bin_p95_tmp <- terra::resample(bin_p95_tmp, template_count, method = "near")
      }

      terra::values(bin_mean_rast) <- terra::values(bin_mean_tmp)
      terra::values(bin_sd_rast)   <- terra::values(bin_sd_tmp)
      terra::values(bin_p95_rast)  <- terra::values(bin_p95_tmp)
    }

    # Density
    bin_density_rast <- bin_count_rast / vertical_cell_area_m2

    # Percent of veg in this bin (veg-only denominator)
    bin_pct_rast <- bin_count_rast / veg_count_rast
    bin_pct_rast[!is.finite(bin_pct_rast)] <- 0  # denom=0 => 0

    # Write
    safe_write(bin_density_rast, file.path(out_dir, paste0(tile_key, "_dens_5m.tif")))
    safe_write(bin_pct_rast,     file.path(out_dir, paste0(tile_key, "_pct_5m.tif")))
    safe_write(bin_mean_rast,    file.path(out_dir, paste0(tile_key, "_meanZ_5m.tif")))
    safe_write(bin_sd_rast,      file.path(out_dir, paste0(tile_key, "_sdZ_5m.tif")))
    safe_write(bin_p95_rast,     file.path(out_dir, paste0(tile_key, "_p95Z_5m.tif")))
  }

  invisible(NULL)
}

####### Step 10 RUNS #######

###### Step 10a ######
###### RUN VERTICAL BINS: UTM11 ######
for (single_norm_path in utm11_norm_files_for_loop) {
  compute_and_write_vertical_bins(single_norm_path, utm11_dirs$vertical_bins_5m)
}

###### Step 10b ######
###### RUN VERTICAL BINS: UTM12 ######
for (single_norm_path in utm12_norm_files_for_loop) {
  compute_and_write_vertical_bins(single_norm_path, utm12_dirs$vertical_bins_5m)
}

###### Step 10c ######
###### QA: Do bin % rasters sum to ~1 per pixel? ######

qa_check_bin_pct_sum <- function(vertical_bins_dir_list,
                                 summary_dir = vertical_bins_dir_list$summary,
                                 tol = 0.02) {
  # tol = allowable absolute deviation from 1.0 (e.g., 0.02 = 2%)

  # For each tile, your rasters live in per-bin folders.
  # We find matching *_pct_5m.tif files for each bin and group by base_name.

  pct0_files <- list.files(vertical_bins_dir_list$bin_0to2m,   pattern = "_pct_5m\\.tif$", full.names = TRUE, ignore.case = TRUE)
  pct2_files <- list.files(vertical_bins_dir_list$bin_2to5m,   pattern = "_pct_5m\\.tif$", full.names = TRUE, ignore.case = TRUE)
  pct5_files <- list.files(vertical_bins_dir_list$bin_5to10m,  pattern = "_pct_5m\\.tif$", full.names = TRUE, ignore.case = TRUE)
  pct10_files<- list.files(vertical_bins_dir_list$bin_10mplus, pattern = "_pct_5m\\.tif$", full.names = TRUE, ignore.case = TRUE)

  stopifnot(length(pct0_files) > 0, length(pct2_files) > 0, length(pct5_files) > 0, length(pct10_files) > 0)

  # Helper to get base id = everything before "_pct_5m.tif"
  get_id <- function(x) sub("_pct_5m\\.tif$", "", basename(x), ignore.case = TRUE)

  ids0  <- vapply(pct0_files,  get_id, character(1))
  ids2  <- vapply(pct2_files,  get_id, character(1))
  ids5  <- vapply(pct5_files,  get_id, character(1))
  ids10 <- vapply(pct10_files, get_id, character(1))

  common_ids <- Reduce(intersect, list(ids0, ids2, ids5, ids10))
  if (length(common_ids) == 0) stop("No matching *_pct_5m.tif sets found across the 4 bins.")

  message("QA: found ", length(common_ids), " tiles with all 4 bin pct rasters.")

  # Loop tile-by-tile
  for (id in common_ids) {

    f0  <- pct0_files[ids0  == id][1]
    f2  <- pct2_files[ids2  == id][1]
    f5  <- pct5_files[ids5  == id][1]
    f10 <- pct10_files[ids10 == id][1]

    r0  <- terra::rast(f0)
    r2  <- terra::rast(f2)
    r5  <- terra::rast(f5)
    r10 <- terra::rast(f10)

    # Sum of bin percentages
    r_sum <- r0 + r2 + r5 + r10
    r_err <- abs(r_sum - 1)

    # If you wrote a veg density raster in summary, use it to split "veg present" vs "no veg"
    # (You may have _dens_total_5m.tif; ideally you'd have _dens_veg_total_5m.tif, but we can use total density as a proxy.)
    
vegcount_path <- file.path(summary_dir, paste0(id, "_vegcount_5m.tif"))
vegcount_r <- if (file.exists(vegcount_path)) terra::rast(vegcount_path) else NULL

if (!is.null(vegcount_r)) {
  # Pixels with vegetation returns (correct mask for your veg-only bins)
  mask_has_pts <- vegcount_r > 0
} else {
  # Fallback: if vegcount wasn't written, just use non-NA sum pixels
  mask_has_pts <- !is.na(r_sum)
}

    # Stats: where there are points, does it sum close to 1?
    vals_sum <- terra::values(r_sum, mat = FALSE)
    vals_err <- terra::values(r_err, mat = FALSE)
    vals_mask <- terra::values(mask_has_pts, mat = FALSE)

    ok_idx <- which(!is.na(vals_sum) & !is.na(vals_mask) & vals_mask)

    if (length(ok_idx) == 0) {
      message("QA SKIP: no valid pixels for ", id)
      next
    }

    mean_sum <- mean(vals_sum[ok_idx], na.rm = TRUE)
    max_err  <- max(vals_err[ok_idx],  na.rm = TRUE)
    pct_bad  <- mean(vals_err[ok_idx] > tol, na.rm = TRUE) * 100

    message("QA ", id, " | mean(sum)=", round(mean_sum, 4),
            " | max|sum-1|=", round(max_err, 4),
            " | %pixels>|tol|=", round(pct_bad, 2), "%")

    # Write rasters for visual QA in QGIS
    terra::writeRaster(r_sum,
      filename = file.path(summary_dir, paste0(id, "_QA_binsum_pct_5m.tif")),
      overwrite = TRUE
    )

    terra::writeRaster(r_err,
      filename = file.path(summary_dir, paste0(id, "_QA_binsum_absErr_5m.tif")),
      overwrite = TRUE
    )
  }

  invisible(NULL)
}

###### Step 10c1 ######
###### RUN QA: UTM11 ######
qa_check_bin_pct_sum(utm11_dirs$vertical_bins_5m)

###### Step 10c2 ######
###### RUN QA: UTM12 ######
qa_check_bin_pct_sum(utm12_dirs$vertical_bins_5m)

###### Step 11 ######
###### Rugosity from CHM (SD aggregated to 5m) ######

compute_rugosity_from_chm_folder <- function(chm_folder, rugosity_folder) {

  chm_paths <- list.files(
    chm_folder,
    pattern = "_chm\\.(tif|grd)$",
    full.names = TRUE,
    ignore.case = TRUE
  )

  stopifnot(length(chm_paths) > 0)

  for (single_chm_path in chm_paths) {

    chm_1m <- terra::rast(single_chm_path)

    # SD of 1m CHM aggregated to 5m
    rugosity_sd_5m <- terra::aggregate(chm_1m, fact = 5, fun = sd, na.rm = TRUE)

    base_name <- tools::file_path_sans_ext(basename(single_chm_path))

    terra::writeRaster(
      rugosity_sd_5m,
      filename = file.path(rugosity_folder, paste0(base_name, "_rugosity_sd_5m.tif")),
      overwrite = TRUE
    )
  }

  invisible(NULL)
}

compute_rugosity_from_chm_folder(utm11_dirs$chm_res1m, utm11_dirs$rugosity_5m)
compute_rugosity_from_chm_folder(utm12_dirs$chm_res1m, utm12_dirs$rugosity_5m)

###### Step 12 ######
###### Complexity (1m) (BOTH) ######

complex_fun <- function(cluster, ...) {

  las_object <- lidR::readLAS(cluster)
  if (lidR::is.empty(las_object)) return(NULL)

  las_canopy_only <- lidR::filter_poi(las_object, Classification != 2L & Z > 0)

  complexity_raster <- lidR::pixel_metrics(las_canopy_only, ~ sd(Z) / mean(Z), res = 1)

  return(complexity_raster)
}

###### Step 12a ######
###### COMPLEXITY: UTM11 ######

lidR::opt_output_files(ctg_norm_utm11) <- file.path(
  utm11_dirs$complexity_res1m,
  "{ORIGINALFILENAME}_complexity"
)

lidR::catalog_apply(ctg_norm_utm11, complex_fun)

###### Step 12b ######
###### COMPLEXITY: UTM12 ######

lidR::opt_output_files(ctg_norm_utm12) <- file.path(
  utm12_dirs$complexity_res1m,
  "{ORIGINALFILENAME}_complexity"
)

lidR::catalog_apply(ctg_norm_utm12, complex_fun)

###### Step 13 ######
###### FINAL QA: PRINT WHERE OUTPUTS WERE SAVED ######

cat("\nDONE.\n")
cat("All UTM11 outputs saved under: ", metrics_utm11_root_dir, "\n")
cat("All UTM12 outputs saved under: ", metrics_utm12_root_dir, "\n")

cat("\nUTM11 subfolders:\n"); print(utm11_dirs)
cat("\nUTM12 subfolders:\n"); print(utm12_dirs)
```



# Full Study Area Batched

Now that the pipeline is defined and tested on a few tiles. We can apply it to our study area with a few adjustments. We can use the *future* package to parallel process the liDAR data and try to work through the pipeline faster. We will start by setting up this process and creating a place to save temp files. Temp files are a just-in-case measure since most of the data is connecting to a network. I will be saving the fully processed tiles in the network drive in case we have to access them later from a different computer.

## Setup

```{r Setup Parallel Processing and Temp Files}
# My lidR version uses this for catalog parallelism. This code allows for 8 tiles to be processed at 1 time. 
options(lidR.cores = 8) # number of tiles processed in parallel
lidR::set_lidr_threads(1) # keep internal threads low to avoid RAM spikes per worker

# Keep temporary files local 
dir.create("C:/lidar_work/temp", recursive = TRUE, showWarnings = FALSE)
Sys.setenv(TMPDIR = "C:/lidar_work/temp")

# Create directory to save our data in our network drive
run_root <- "R:/CACR_HSM_LiDAR/Output_Files/FullArea_Run" # creates a base folder that we can come back to
step_dirs <- c(
  "01_filtered",
  "02_ground_csf",
  "03_dem",
  "04_normalized",
  "05_chm",
  "06_metrics",
  "logs"
) # lists each pipeline step

for (d in step_dirs) {
  dir.create(file.path(run_root, d), recursive = TRUE, showWarnings = FALSE)
} # Create a folder for each step 
```

Next we want to direct R to where our files live. As a note, each folder is the entire laz dataset for a single flight. File names are not consistent so we will have to tell R to take any LAZ file from within our network drive (R:/). Our shapefile for our study area is also found in this network drive and we will want to load it in because we only want to spend time on tiles that intersect our study area.

## Load Data

```{r Load Tiles and Shapefiles}
# Save pathway to the shapefile (note: there are two polygons)
study_area_path <- "R:/Shapefiles/StudyArea.shp"

# Load Study area shapefile
study_area <- st_read(study_area_path, quiet = TRUE)

# Validate the study area
study_area <- st_make_valid(study_area)

study_area <- st_union(study_area)


# Root where ALL flights/tiles live (this is across multiple folders)
lidar_root <- "R:/"

# Recursively find ALL LAZ/LAS tiles in the R network drive
tile_files <- list.files(
  lidar_root,
  pattern = "\\.(laz|las)$",
  recursive = TRUE, 
  full.names = TRUE
)

length(tile_files)
tile_files[1:10]
```

We will want to verify that the shapefile and LAZ/LAS files are in the same CRS otherwise this whole thing will fail.

```{r Verify CRS}
# CRS of the shapefile should be EPSG: 8826 but let's confirm
st_crs(study_area)

# Try to find LAZ CRS
h1 <- lidR::readLASheader(tile_files[1])
las_has_crs <- FALSE
try({
     crs_guess <- lidR::st_crs(h1)
     if (!is.na(crs_guess)) {
         print(crs_guess)
         las_has_crs <- TRUE
     }
 }, silent = TRUE)

# 1) Pick a representative LAZ from your dataset (any one is fine)
one_tile <- tile_files[1]

# 2) Read CRS from LAZ header
h1 <- lidR::readLASheader(one_tile)
crs_lidar <- lidR::st_crs(h1)   # you already proved this works

crs_lidar  # print it

# Transform study area to match LAZ
if (is.na(st_crs(study_area))) stop("Study shapefile CRS is NA in R (undefined). Fix it before transforming.")

study_area <- st_transform(study_area, crs_lidar)
st_crs(study_area) == st_crs(h1) #asks true or false if they match
```

## Find Only Tiles in Study Area

We do not want to process the entire dataset. We ONLY want those that fall within our study area, so we are going to use the st_intersection function to find only tiles in our study area. Instead of loading in every point cloud or cataloging all of the tiles, we are going to create a table that lists the bounding box of each LAZ/LAS file and use that to find which overlap.

```{r Find the Tiles!}
# Build a footprint (bbox polygon) for every LAZ/LAS using the header
study_union <- st_union(study_area)

# --- single pass header read (with tryCatch) ---
tile_dt <- rbindlist(lapply(tile_files, function(f) {
  tryCatch({
    h <- lidR::readLASheader(f)
    data.table(
      file = f,
      ok   = TRUE,
      xmin = h@PHB[["Min X"]],
      xmax = h@PHB[["Max X"]],
      ymin = h@PHB[["Min Y"]],
      ymax = h@PHB[["Max Y"]]
    )
  }, error = function(e) {
    data.table(file = f, ok = FALSE)
  })
}), fill = TRUE)

# --- sanity: header failures ---
sum(!tile_dt$ok)
# Save immediately so you don’t redo this step
saveRDS(tile_dt, "R:/CACR_HSM_LiDAR/Output_Files/FullArea_Run/logs/tile_dt_headers.rds")

# --- sanity: bad extents ---
bad_extent <- tile_dt[ok == TRUE & (xmax <= xmin | ymax <= ymin | is.na(xmin) | is.na(ymin))]
nrow(bad_extent)
# Optional: save a list of problematic files
if (nrow(bad_extent) > 0) {
  writeLines(bad_extent$file, "R:/CACR_HSM_LiDAR/Output_Files/FullArea_Run/logs/bad_extent_files.txt")
}

# --- build polygons only for good headers ---
tile_ok <- tile_dt[ok == TRUE]

tile_sf <- st_as_sf(tile_ok, coords = c("xmin","ymin"), crs = crs_lidar) # dummy
tile_sf$geometry <- st_sfc(lapply(seq_len(nrow(tile_ok)), function(i) {
  st_polygon(list(matrix(
    c(tile_ok$xmin[i], tile_ok$ymin[i],
      tile_ok$xmax[i], tile_ok$ymin[i],
      tile_ok$xmax[i], tile_ok$ymax[i],
      tile_ok$xmin[i], tile_ok$ymax[i],
      tile_ok$xmin[i], tile_ok$ymin[i]),
    ncol = 2, byrow = TRUE
  )))
}), crs = crs_lidar)

# --- sanity: CRS match ---
stopifnot(st_crs(tile_sf) == st_crs(study_union))

# --- intersects ---
hits <- st_intersects(tile_sf, study_union, sparse = FALSE)[, 1]
keep_files <- tile_sf$file[hits]

length(tile_files)
length(keep_files)

# Save keep list for tomorrow
saveRDS(keep_files, "R:/CACR_HSM_LiDAR/Output_Files/FullArea_Run/logs/keep_files.rds")
writeLines(keep_files, "R:/CACR_HSM_LiDAR/Output_Files/FullArea_Run/logs/keep_files.txt")
```

## Filter Tiles

```{r Filter Tiles}
# Read in kept tiles
keep_files <- readRDS("R:/CACR_HSM_LiDAR/Output_Files/FullArea_Run/logs/keep_files.rds")

# 1) What is it?
class(keep_files)
length(keep_files)
head(keep_files, 3)

# 2) Do the files actually exist from this machine?
all(file.exists(keep_files))
sum(!file.exists(keep_files))    # should be 0

# Catalog from selected tiles
ctg_filter_AOI <- readLAScatalog(keep_files)

## Set up Filter
# One output per input tile
opt_chunk_size(ctg_filter_AOI)   <- 0
opt_chunk_buffer(ctg_filter_AOI) <- 0

# Parallel + stability
options(lidR.cores = 8)
lidR::set_lidr_threads(1)

# Output folders (on R:)
run_root <- "R:/CACR_HSM_LiDAR/Output_Files/FullArea_Run"
out_filtered <- file.path(run_root, "01_filtered")
log_dir <- file.path(run_root, "logs")
dir.create(out_filtered, recursive = TRUE, showWarnings = FALSE)
dir.create(log_dir, recursive = TRUE, showWarnings = FALSE)

# Unique naming (prevents collisions across flights)
opt_output_files(ctg_filter_AOI) <- file.path(out_filtered, "{ORIGINALFILENAME}_{ID}_filtered")

# Filter function (won't crash the whole run if 1 tile fails)
filter_fun_AOI <- function(x, ...) {
  tryCatch({
    if (inherits(x, "LAScluster")) x <- readLAS(x)
    if (is.null(x) || is.empty(x)) return(NULL)

    x <- filter_poi(x, !Withheld_flag & !Synthetic_flag)
    x <- classify_noise(x, ivf(res = 4, n = 12))
    x <- filter_poi(x, !Classification %in% c(7L, 18L))

    if (is.empty(x)) return(NULL)
    x
  }, error = function(e) {
    cat(Sys.time(), "- ERROR:", conditionMessage(e), "\n",
        file = file.path(log_dir, "filter_errors.txt"),
        append = TRUE)
    NULL
  })
}

# Run (you'll see the lidR progress bar)
Filter_AOI <- catalog_apply(ctg_filter_AOI, filter_fun_AOI)
Filter_AOI

########## BUG (Only shows one of 2 areas) ############

# Build tile footprints from keep_files only (fast)
tile_dt_keep <- data.table::rbindlist(lapply(keep_files, function(f) {
  h <- lidR::readLASheader(f)
  data.table::data.table(
    file = f,
    xmin = h@PHB[["Min X"]],
    xmax = h@PHB[["Max X"]],
    ymin = h@PHB[["Min Y"]],
    ymax = h@PHB[["Max Y"]]
  )
}))

tile_sf_keep <- st_as_sf(tile_dt_keep, coords=c("xmin","ymin"), crs=crs_lidar)
tile_sf_keep$geometry <- st_sfc(lapply(seq_len(nrow(tile_dt_keep)), function(i) {
  st_polygon(list(matrix(
    c(tile_dt_keep$xmin[i], tile_dt_keep$ymin[i],
      tile_dt_keep$xmax[i], tile_dt_keep$ymin[i],
      tile_dt_keep$xmax[i], tile_dt_keep$ymax[i],
      tile_dt_keep$xmin[i], tile_dt_keep$ymax[i],
      tile_dt_keep$xmin[i], tile_dt_keep$ymin[i]),
    ncol=2, byrow=TRUE
  )))
}), crs=crs_lidar)

# How many kept tiles intersect each boundary?
n1 <- sum(st_intersects(tile_sf_keep, study_area[1,], sparse=FALSE)[,1])
n2 <- sum(st_intersects(tile_sf_keep, study_area[2,], sparse=FALSE)[,1])
c(boundary1_tiles = n1, boundary2_tiles = n2)
plot(st_geometry(tile_sf_keep), border="grey", lwd=0.2)
plot(st_geometry(study_area), add=TRUE, border="red", lwd=2)
```

### Debug Albion Missing Tiles ####

We can see the albion tiles weren't captured in our earlier extraction. We need to figure out the reason and get the albion tiles back in the pipeline.

Files were not captured due to differences in CRS between flights that covered the South Hills and the Ablions. Best course of action is to keep them in separate CRS throughout the pipeline (run them and save them separatly). We will keep the file directory for the South Hills, create a new one for the albions, filter the Albions, and continue down the pipeline.

**After metric rasters area created, we will reproject them into the same CRS.**

```{r Debug missing tiles}
############# STEP: LiDAR Inventory + CRS Audit (HEADER ONLY) ############
## Purpose:
##   1) Confirm ALL flights share ONE CRS (or detect multiple)
##   2) Confirm we have tile coverage candidates near Study Area 2
##   3) Produce saved audit tables for reproducibility
##
## This does NOT re-filter or write any LAS outputs.

######### 0) Define durable paths ###########

pipeline_run_root_directory <- "R:/CACR_HSM_LiDAR/Output_Files/FullArea_Run"
pipeline_log_directory      <- file.path(pipeline_run_root_directory, "logs")
dir.create(pipeline_log_directory, recursive = TRUE, showWarnings = FALSE)

####### 1) Full tile inventory (do not overwrite generic names) ######

lidar_all_tile_paths_full_inventory <- tile_files  # your existing discovered list

if (length(lidar_all_tile_paths_full_inventory) == 0) {
  stop("lidar_all_tile_paths_full_inventory is empty. Tile discovery failed.")
}

######### 2) Read Study Area shapefile while preserving polygon rows #########
##    DO NOT st_union() this object; we need Albion Section explicitly.

study_area_shapefile_path <- "R:/Shapefiles/StudyArea.shp"
study_area_features_raw   <- st_read(study_area_shapefile_path, quiet = TRUE)
study_area_features_raw   <- st_make_valid(study_area_features_raw)

# Ensure we have explicit polygon rows (so [2,] really means boundary 2)
study_area_polygon_rows <- st_cast(study_area_features_raw, "POLYGON", warn = FALSE)

# Prefer attribute-based selection for reproducibility
study_area_polygon_albion <- study_area_polygon_rows[study_area_polygon_rows$Section == "Albion", ]
if (nrow(study_area_polygon_albion) != 1) stop("Albion selection did not return.")

study_area_polygon_albion <- study_area_polygon_rows[study_area_polygon_rows$Section == "Albion", ]

######### 3) Build header inventory table INCLUDING CRS per tile ###########
##    This detects multi-CRS issues across flights.

lidar_header_inventory_table <- rbindlist(lapply(lidar_all_tile_paths_full_inventory, function(single_tile_path) {
  tryCatch({
    tile_header_object <- lidR::readLASheader(single_tile_path)

    # Extract extents
    tile_xmin <- tile_header_object@PHB[["Min X"]]
    tile_xmax <- tile_header_object@PHB[["Max X"]]
    tile_ymin <- tile_header_object@PHB[["Min Y"]]
    tile_ymax <- tile_header_object@PHB[["Max Y"]]

    # CRS from header (may be NA)
    tile_crs_object <- tryCatch(lidR::st_crs(tile_header_object), error = function(e) NA)

    data.table(
      tile_path          = single_tile_path,
      header_read_ok     = TRUE,
      xmin               = tile_xmin,
      xmax               = tile_xmax,
      ymin               = tile_ymin,
      ymax               = tile_ymax,
      crs_epsg           = if (is.na(tile_crs_object)) NA_integer_ else tile_crs_object$epsg,
      crs_wkt            = if (is.na(tile_crs_object)) NA_character_ else tile_crs_object$wkt,
      flight_folder_name = sub("^R:/([^/]+)/.*", "\\1", single_tile_path)
    )
  }, error = function(e) {
    data.table(
      tile_path          = single_tile_path,
      header_read_ok     = FALSE,
      xmin               = NA_real_,
      xmax               = NA_real_,
      ymin               = NA_real_,
      ymax               = NA_real_,
      crs_epsg           = NA_integer_,
      crs_wkt            = NA_character_,
      flight_folder_name = sub("^R:/([^/]+)/.*", "\\1", single_tile_path)
    )
  })
}), fill = TRUE)

## Save inventory for reproducibility (you never have to re-run header reads)
saveRDS(
  lidar_header_inventory_table,
  file.path(pipeline_log_directory, "lidar_header_inventory_full.rds")
)

cat("Header read failures:", sum(!lidar_header_inventory_table$header_read_ok), "\n")

###### 4) Flight tile counts (informational; helps spot missing flights) ##########

lidar_tiles_per_flight_folder_table <- lidar_header_inventory_table[
  , .(tile_count = .N,
      header_failures = sum(!header_read_ok)),
  by = flight_folder_name
][order(-tile_count)]

print(lidar_tiles_per_flight_folder_table)

saveRDS(
  lidar_tiles_per_flight_folder_table,
  file.path(pipeline_log_directory, "lidar_tiles_per_flight_folder_table.rds")
)

##### 5) CRS audit: hard stop if multiple CRSs present among readable tiles ##########

lidar_readable_tiles_table <- lidar_header_inventory_table[header_read_ok == TRUE]

unique_crs_wkt_values <- unique(na.omit(lidar_readable_tiles_table$crs_wkt))

cat("Distinct CRS (WKT) among readable tiles:", length(unique_crs_wkt_values), "\n")

if (length(unique_crs_wkt_values) == 0) {
  stop("No readable CRS info found in headers. CRS is missing from tiles; cannot guarantee alignment.")
}

if (length(unique_crs_wkt_values) > 1) {
  # Show which flights have which CRS
  crs_by_flight_table <- lidar_readable_tiles_table[
    , .(n_tiles = .N),
    by = .(flight_folder_name, crs_epsg)
  ][order(flight_folder_name, crs_epsg)]

  print(crs_by_flight_table)

  saveRDS(
    crs_by_flight_table,
    file.path(pipeline_log_directory, "crs_by_flight_table.rds")
  )

  stop("MULTIPLE LiDAR CRSs detected across flights. Do NOT mix these in one pipeline run. Split by CRS or reproject.")
}

# See 3 different CRS used

# Input: lidar_header_inventory_table already exists in your session
# Output: lidar_crs_group_by_flight_table + saved RDS

lidar_readable_tiles_table <- lidar_header_inventory_table[header_read_ok == TRUE]

# Create a stable CRS "group id" based on WKT text.
# (We use a short hash-like id derived from the first 60 chars + nchar for readability.)
lidar_readable_tiles_table[, crs_wkt_prefix := substr(crs_wkt, 1, 60)]
lidar_readable_tiles_table[, crs_wkt_length := nchar(crs_wkt)]

lidar_crs_group_by_flight_table <- lidar_readable_tiles_table[
  , .(n_tiles = .N),
  by = .(flight_folder_name, crs_wkt_prefix, crs_wkt_length)
][order(flight_folder_name, -n_tiles)]

print(lidar_crs_group_by_flight_table)

saveRDS(
  lidar_crs_group_by_flight_table,
  file.path(pipeline_log_directory, "lidar_crs_group_by_flight_table.rds")
)

##   - Identify which of the 3 CRS WKT strings equals the target CRS

target_lidar_crs_object <- crs_lidar  # reuse your already-defined target CRS object
target_wkt_string       <- target_lidar_crs_object$wkt

lidar_distinct_crs_table <- data.table(
  crs_wkt = unique(na.omit(lidar_readable_tiles_table$crs_wkt))
)

lidar_distinct_crs_table[, wkt_equals_target := (crs_wkt == target_wkt_string)]
lidar_distinct_crs_table[, crs_wkt_prefix := substr(crs_wkt, 1, 80)]
lidar_distinct_crs_table[, crs_wkt_length := nchar(crs_wkt)]

print(lidar_distinct_crs_table[, .(wkt_equals_target, crs_wkt_prefix, crs_wkt_length)])

# Same as your target CRS (UTM zone 11N + NAVD88 height Geoid12B) ✅

# 07_Cassia — 142 tiles
# Part21 — 313 tiles
# Part22 — 472 tiles
# ElkoNW_LAZ — mostly target CRS, but mixed (see below)

# Different CRS (UTM zone 12N + NAVD88 height Geoid12B) ❌

# LAZ — 5,449 tiles (zone 12N)
# SouthernIdaho2018_14_LAZ — 3,299 tiles (zone 12N)

saveRDS(lidar_distinct_crs_table, file.path(pipeline_log_directory, "lidar_distinct_crs_table.rds"))

## COORDINATE RANGE CHECK BY CRS GROUP (SAFE)
## Purpose: 1) Detect whether CRS groups are truly in different coordinate spaces and 2) If ranges overlap tightly, differences may be metadata-only

coordinate_ranges_by_crs_table <- lidar_readable_tiles_table[
  , .(
    xmin_min = min(xmin, na.rm = TRUE),
    xmax_max = max(xmax, na.rm = TRUE),
    ymin_min = min(ymin, na.rm = TRUE),
    ymax_max = max(ymax, na.rm = TRUE),
    n_tiles  = .N
  ),
  by = .(crs_wkt_prefix, crs_wkt_length)
][order(-n_tiles)]

print(coordinate_ranges_by_crs_table)
saveRDS(coordinate_ranges_by_crs_table, file.path(pipeline_log_directory, "coordinate_ranges_by_crs_table.rds"))

##### STEP 6: Albion coverage audit by CRS group (HEADER ONLY) ########
## Purpose:
##   - Identify which CRS group(s) contain tiles near Albion
##   - Avoid reprojecting or processing unnecessary tiles
##   - Save candidate lists to logs for reproducibility
##
## Outputs:
##   - logs/albion_tile_candidates_header_only.rds
##   - logs/albion_tile_candidates_summary_by_crs_group.rds

# Create a stable CRS group label for each tile (explicit, human-readable)
albion_tile_candidate_inventory_table <- data.table::copy(lidar_readable_tiles_table)

albion_tile_candidate_inventory_table[, crs_group_label := data.table::fifelse(
  grepl("UTM zone 12N", crs_wkt, fixed = TRUE), "utm12_geoid12B",
  data.table::fifelse(
    grepl("Geoid18", crs_wkt, fixed = TRUE), "utm11_geoid18",
    "utm11_geoid12B_target"
  )
)]

# We will bbox-filter Albion separately within each CRS group
albion_tile_candidate_inventory_table[, is_within_albion_bbox_in_that_crs := FALSE]

# Loop CRS groups to transform Albion polygon into each group CRS and do a fast bbox overlap test
for (single_crs_group_label in unique(albion_tile_candidate_inventory_table$crs_group_label)) {

  # Pull one example WKT to define the CRS object for this group
  example_wkt_for_group <- albion_tile_candidate_inventory_table[
    crs_group_label == single_crs_group_label, crs_wkt
  ][1]

  crs_object_for_group <- sf::st_crs(example_wkt_for_group)

  # Transform Albion polygon into this CRS group for apples-to-apples bbox comparison
  albion_polygon_in_group_crs <- sf::st_transform(study_area_polygon_albion, crs_object_for_group)
  albion_bbox_in_group_crs    <- sf::st_bbox(albion_polygon_in_group_crs)

  # Flag tiles in this CRS group whose header bbox overlaps Albion bbox
  albion_tile_candidate_inventory_table[
    crs_group_label == single_crs_group_label &
      !(
        xmax < albion_bbox_in_group_crs["xmin"] |
        xmin > albion_bbox_in_group_crs["xmax"] |
        ymax < albion_bbox_in_group_crs["ymin"] |
        ymin > albion_bbox_in_group_crs["ymax"]
      ),
    is_within_albion_bbox_in_that_crs := TRUE
  ]
}

# Subset to just Albion bbox candidates (still header-only)
albion_tile_candidates_header_only_table <- albion_tile_candidate_inventory_table[
  is_within_albion_bbox_in_that_crs == TRUE
]

# Summarize: where is Albion coverage likely coming from?
albion_tile_candidates_summary_by_crs_group <- albion_tile_candidates_header_only_table[
  , .(n_candidate_tiles = .N),
  by = .(crs_group_label, flight_folder_name)
][order(crs_group_label, -n_candidate_tiles)]

print(albion_tile_candidates_summary_by_crs_group)

# Save for reproducibility
saveRDS(
  albion_tile_candidates_header_only_table,
  file.path(pipeline_log_directory, "albion_tile_candidates_header_only.rds")
)

saveRDS(
  albion_tile_candidates_summary_by_crs_group,
  file.path(pipeline_log_directory, "albion_tile_candidates_summary_by_crs_group.rds")
)

###### STEP 7: Build TRUE intersecting Albion keep lists (HEADER ONLY) ##########
## Purpose:
##   - Convert candidate tile header bboxes to footprint polygons
##   - Perform true intersection with Albion (in each CRS group)
##   - Save definitive keep lists per CRS group
##
## Outputs:
##   - logs/keep_tiles_albion_by_crs_group_<group>.rds
##   - logs/keep_tiles_albion_by_crs_group_<group>.txt

albion_tile_candidates_header_only_table <- readRDS(
  file.path(pipeline_log_directory, "albion_tile_candidates_header_only.rds")
)

# For each CRS group, build footprints and intersect in that CRS
for (single_crs_group_label in unique(albion_tile_candidates_header_only_table$crs_group_label)) {

  albion_candidates_in_group_table <- albion_tile_candidates_header_only_table[
    crs_group_label == single_crs_group_label
  ]

  # CRS object for this group
  example_wkt_for_group <- albion_candidates_in_group_table$crs_wkt[1]
  crs_object_for_group  <- sf::st_crs(example_wkt_for_group)

  # Albion polygon transformed into this group CRS
  albion_polygon_in_group_crs <- sf::st_transform(study_area_polygon_albion, crs_object_for_group)

  # Build footprint polygons from header extents
  albion_candidate_footprints_sf <- sf::st_as_sf(
    albion_candidates_in_group_table,
    coords = c("xmin","ymin"),
    crs = crs_object_for_group
  )

  albion_candidate_footprints_sf$geometry <- sf::st_sfc(lapply(seq_len(nrow(albion_candidates_in_group_table)), function(i) {
    sf::st_polygon(list(matrix(
      c(albion_candidates_in_group_table$xmin[i], albion_candidates_in_group_table$ymin[i],
        albion_candidates_in_group_table$xmax[i], albion_candidates_in_group_table$ymin[i],
        albion_candidates_in_group_table$xmax[i], albion_candidates_in_group_table$ymax[i],
        albion_candidates_in_group_table$xmin[i], albion_candidates_in_group_table$ymax[i],
        albion_candidates_in_group_table$xmin[i], albion_candidates_in_group_table$ymin[i]),
      ncol = 2, byrow = TRUE
    )))
  }), crs = crs_object_for_group)

  # True intersection
  albion_true_intersects_logical <- lengths(sf::st_intersects(albion_candidate_footprints_sf, albion_polygon_in_group_crs)) > 0

  albion_keep_tile_paths_in_group <- albion_candidate_footprints_sf$tile_path[albion_true_intersects_logical]

  cat("Albion true intersect tiles for", single_crs_group_label, ":", length(albion_keep_tile_paths_in_group), "\n")

  # Save keep list
  keep_list_rds_path <- file.path(
    pipeline_log_directory,
    paste0("keep_tiles_albion_by_crs_group_", single_crs_group_label, ".rds")
  )

  keep_list_txt_path <- file.path(
    pipeline_log_directory,
    paste0("keep_tiles_albion_by_crs_group_", single_crs_group_label, ".txt")
  )

  saveRDS(albion_keep_tile_paths_in_group, keep_list_rds_path)
  writeLines(albion_keep_tile_paths_in_group, keep_list_txt_path)
}

# showing only 191 tiles but QGIS shows 232 total for flights 17 and 14

######## 5A: Albion TRUE intersect from ALL UTM12 tiles (NO header rereads) ########
## Purpose:
##   - Remove any possibility that bbox-candidate logic dropped tiles
##   - Use existing lidar_header_inventory_table extents as footprints
##   - Produce authoritative counts by flight folder
##
## Inputs assumed to exist:
##   - lidar_header_inventory_table
##   - study_area_polygon_albion
##   - pipeline_log_directory


# 1) Identify the exact UTM12 CRS WKT from your distinct CRS table (no guessing)
utm12_geoid12B_wkt_string <- lidar_distinct_crs_table[
  grepl('UTM zone 12N', crs_wkt, fixed = TRUE) & grepl('Geoid12B', crs_wkt, fixed = TRUE),
  crs_wkt
][1]

if (is.na(utm12_geoid12B_wkt_string) || length(utm12_geoid12B_wkt_string) == 0) {
  stop("Could not find UTM zone 12N + Geoid12B CRS WKT in lidar_distinct_crs_table.")
}

utm12_geoid12B_crs_object <- sf::st_crs(utm12_geoid12B_wkt_string)

# 2) Subset header inventory to just UTM12 tiles (this should be LAZ + SouthernIdaho2018_14_LAZ)
utm12_tile_header_inventory_table <- lidar_header_inventory_table[
  header_read_ok == TRUE & crs_wkt == utm12_geoid12B_wkt_string
]

# 3) Transform Albion polygon into UTM12 CRS
albion_polygon_in_utm12_crs <- sf::st_transform(study_area_polygon_albion, utm12_geoid12B_crs_object)

# 4) Build footprint polygons from header extents (bbox polygons)
utm12_tile_footprints_sf <- sf::st_as_sf(
  utm12_tile_header_inventory_table,
  coords = c("xmin", "ymin"),
  crs = utm12_geoid12B_crs_object
)

utm12_tile_footprints_sf$geometry <- sf::st_sfc(lapply(seq_len(nrow(utm12_tile_header_inventory_table)), function(i) {
  sf::st_polygon(list(matrix(
    c(utm12_tile_header_inventory_table$xmin[i], utm12_tile_header_inventory_table$ymin[i],
      utm12_tile_header_inventory_table$xmax[i], utm12_tile_header_inventory_table$ymin[i],
      utm12_tile_header_inventory_table$xmax[i], utm12_tile_header_inventory_table$ymax[i],
      utm12_tile_header_inventory_table$xmin[i], utm12_tile_header_inventory_table$ymax[i],
      utm12_tile_header_inventory_table$xmin[i], utm12_tile_header_inventory_table$ymin[i]),
    ncol = 2, byrow = TRUE
  )))
}), crs = utm12_geoid12B_crs_object)

# 5) TRUE intersects
albion_intersects_logical_utm12 <- lengths(sf::st_intersects(utm12_tile_footprints_sf, albion_polygon_in_utm12_crs)) > 0
albion_keep_tile_paths_utm12_truth <- utm12_tile_footprints_sf$tile_path[albion_intersects_logical_utm12]

cat("Albion TRUE intersect tiles in UTM12 (truth set):", length(albion_keep_tile_paths_utm12_truth), "\n")

# 6) Summarize by flight folder
albion_keep_tile_counts_by_flight_folder_table <- utm12_tile_header_inventory_table[
  tile_path %in% albion_keep_tile_paths_utm12_truth,
  .(n_tiles = .N),
  by = flight_folder_name
][order(-n_tiles)]

print(albion_keep_tile_counts_by_flight_folder_table)

# 7) Save outputs for reproducibility
saveRDS(
  albion_keep_tile_paths_utm12_truth,
  file.path(pipeline_log_directory, "keep_tiles_albion_truthset_utm12_geoid12B.rds")
)

writeLines(
  albion_keep_tile_paths_utm12_truth,
  file.path(pipeline_log_directory, "keep_tiles_albion_truthset_utm12_geoid12B.txt")
)

saveRDS(
  albion_keep_tile_counts_by_flight_folder_table,
  file.path(pipeline_log_directory, "albion_keep_tile_counts_by_flight_folder_table.rds")
)
# shows 17 has 162 tiles and 14 has 29

##### 5B: Albion bbox-overlap tile count in UTM12 (upper bound) #####
## Purpose:
##   - Compare bbox-overlap (looser) vs true polygon intersects (tighter)

albion_bbox_in_utm12_crs <- sf::st_bbox(albion_polygon_in_utm12_crs)

albion_bbox_overlap_logical_utm12 <- !(
  utm12_tile_header_inventory_table$xmax < albion_bbox_in_utm12_crs["xmin"] |
  utm12_tile_header_inventory_table$xmin > albion_bbox_in_utm12_crs["xmax"] |
  utm12_tile_header_inventory_table$ymax < albion_bbox_in_utm12_crs["ymin"] |
  utm12_tile_header_inventory_table$ymin > albion_bbox_in_utm12_crs["ymax"]
)

albion_bbox_overlap_tile_paths_utm12 <- utm12_tile_header_inventory_table$tile_path[albion_bbox_overlap_logical_utm12]

cat("Albion bbox-overlap tiles in UTM12 (upper bound):", length(albion_bbox_overlap_tile_paths_utm12), "\n")

# It will be better to treat these 


##### Create new root for Albion (create new folders similar to ones created earlier) ####

albion_run_root_directory <- "R:/CACR_HSM_LiDAR/Output_Files/FullArea_Run/Albion_Run_UTM12"

pipeline_step_directories <- c(
  "01_filtered",
  "02_ground_csf",
  "03_dem",
  "04_normalized",
  "05_chm",
  "06_metrics",
  "logs"
)

for (single_step_directory in pipeline_step_directories) {
  dir.create(
    file.path(albion_run_root_directory, single_step_directory),
    recursive = TRUE,
    showWarnings = FALSE
  )
}

############ Step 6A PRE-RUN QA: Albion tile selection (HEADER ONLY) ##################
## Purpose:
##   - Confirm coverage visually BEFORE filtering
##   - Quantify what you’re excluding (and from which flights)
##   - Save tile lists so the expensive step uses a locked input set
##
## Inputs expected (already in your session from earlier work):
##   - utm12_tile_header_inventory_table
##   - albion_polygon_in_utm12_crs
##   - utm12_geoid12B_crs_object
##   - albion_keep_tile_paths_utm12_truth          (191)
##   - albion_bbox_overlap_tile_paths_utm12        (364)
##   - albion_log_directory (or pipeline_log_directory if you prefer)


# --- Durable QA output directory (do not overwrite South Hills logs) ---
albion_qa_log_directory <- file.path(albion_run_root_directory, "logs")
dir.create(albion_qa_log_directory, recursive = TRUE, showWarnings = FALSE)

# --- Define sets ---
albion_tiles_true_intersect_paths <- albion_keep_tile_paths_utm12_truth
albion_tiles_bbox_overlap_paths   <- albion_bbox_overlap_tile_paths_utm12
albion_tiles_bbox_only_paths      <- setdiff(albion_tiles_bbox_overlap_tile_paths_utm12, albion_keep_tile_paths_utm12_truth)

cat("TRUE_INTERSECT tiles:", length(albion_tiles_true_intersect_paths), "\n")
cat("BBOX_OVERLAP tiles:", length(albion_tiles_bbox_overlap_paths), "\n")
cat("BBOX_ONLY extra tiles:", length(albion_tiles_bbox_only_paths), "\n")


############ Step 6B PRE-FILTER QA (Albion): 191 TRUE_INTERSECT vs 364 BBOX_OVERLAP ####
## Purpose:
##   - Define the two tile sets explicitly (so objects exist)
##   - Summarize by flight folder
##   - Create a visual footprint plot for each set
## Notes:
##   - Header-only (fast)
##   - Does NOT filter any LAS files


## 0) Safety: confirm required inputs exist

if (!exists("albion_keep_tile_paths_utm12_truth")) stop("Missing albion_keep_tile_paths_utm12_truth (191 TRUE_INTERSECT).")
if (!exists("albion_bbox_overlap_tile_paths_utm12")) stop("Missing albion_bbox_overlap_tile_paths_utm12 (364 BBOX_OVERLAP).")
if (!exists("utm12_tile_header_inventory_table")) stop("Missing utm12_tile_header_inventory_table.")
if (!exists("albion_polygon_in_utm12_crs")) stop("Missing albion_polygon_in_utm12_crs.")
if (!exists("utm12_geoid12B_crs_object")) stop("Missing utm12_geoid12B_crs_object.")
if (!exists("albion_run_root_directory")) stop("Missing albion_run_root_directory.")

albion_qa_log_directory <- file.path(albion_run_root_directory, "logs")
dir.create(albion_qa_log_directory, recursive = TRUE, showWarnings = FALSE)

## 1) Define the two comparison sets (THIS fixes your error)

albion_tiles_true_intersect_paths <- albion_keep_tile_paths_utm12_truth      # 191
albion_tiles_bbox_overlap_paths   <- albion_bbox_overlap_tile_paths_utm12    # 364
albion_tiles_bbox_only_paths      <- setdiff(albion_tiles_bbox_overlap_paths, albion_tiles_true_intersect_paths)

cat("Albion TRUE_INTERSECT tiles:", length(albion_tiles_true_intersect_paths), "\n")
cat("Albion BBOX_OVERLAP tiles:", length(albion_tiles_bbox_overlap_paths), "\n")
cat("Albion BBOX_ONLY extra tiles:", length(albion_tiles_bbox_only_paths), "\n")

## 2) Count by flight folder (191 vs 364, plus the extra-only set)

albion_true_intersect_counts_by_flight_table <- utm12_tile_header_inventory_table[
  tile_path %in% albion_tiles_true_intersect_paths,
  .(n_tiles = .N),
  by = flight_folder_name
][order(-n_tiles)]

albion_bbox_overlap_counts_by_flight_table <- utm12_tile_header_inventory_table[
  tile_path %in% albion_tiles_bbox_overlap_paths,
  .(n_tiles = .N),
  by = flight_folder_name
][order(-n_tiles)]

albion_bbox_only_counts_by_flight_table <- utm12_tile_header_inventory_table[
  tile_path %in% albion_tiles_bbox_only_paths,
  .(n_tiles = .N),
  by = flight_folder_name
][order(-n_tiles)]

cat("\n--- TRUE_INTERSECT counts by flight ---\n")
print(albion_true_intersect_counts_by_flight_table)

cat("\n--- BBOX_OVERLAP counts by flight ---\n")
print(albion_bbox_overlap_counts_by_flight_table)

cat("\n--- BBOX_ONLY (extra) counts by flight ---\n")
print(albion_bbox_only_counts_by_flight_table)

saveRDS(albion_true_intersect_counts_by_flight_table, file.path(albion_qa_log_directory, "qa_true_intersect_counts_by_flight_table.rds"))
saveRDS(albion_bbox_overlap_counts_by_flight_table,   file.path(albion_qa_log_directory, "qa_bbox_overlap_counts_by_flight_table.rds"))
saveRDS(albion_bbox_only_counts_by_flight_table,      file.path(albion_qa_log_directory, "qa_bbox_only_counts_by_flight_table.rds"))


## 3) Build footprint polygons helper (header-only)

build_bbox_footprints_from_header_table <- function(tile_header_table_in, crs_object_in) {

  tile_footprints_sf <- sf::st_as_sf(tile_header_table_in, coords = c("xmin","ymin"), crs = crs_object_in)

  tile_footprints_sf$geometry <- sf::st_sfc(lapply(seq_len(nrow(tile_header_table_in)), function(i) {
    sf::st_polygon(list(matrix(
      c(tile_header_table_in$xmin[i], tile_header_table_in$ymin[i],
        tile_header_table_in$xmax[i], tile_header_table_in$ymin[i],
        tile_header_table_in$xmax[i], tile_header_table_in$ymax[i],
        tile_header_table_in$xmin[i], tile_header_table_in$ymax[i],
        tile_header_table_in$xmin[i], tile_header_table_in$ymin[i]),
      ncol = 2, byrow = TRUE
    )))
  }), crs = crs_object_in)

  tile_footprints_sf
}


## 4) Visual QA plots (191 vs 364, plus extra-only)

albion_true_intersect_header_table <- utm12_tile_header_inventory_table[tile_path %in% albion_tiles_true_intersect_paths]
albion_bbox_overlap_header_table   <- utm12_tile_header_inventory_table[tile_path %in% albion_tiles_bbox_overlap_paths]
albion_bbox_only_header_table      <- utm12_tile_header_inventory_table[tile_path %in% albion_tiles_bbox_only_paths]

albion_true_intersect_footprints_sf <- build_bbox_footprints_from_header_table(albion_true_intersect_header_table, utm12_geoid12B_crs_object)
albion_bbox_overlap_footprints_sf   <- build_bbox_footprints_from_header_table(albion_bbox_overlap_header_table,   utm12_geoid12B_crs_object)
albion_bbox_only_footprints_sf      <- build_bbox_footprints_from_header_table(albion_bbox_only_header_table,      utm12_geoid12B_crs_object)

plot(sf::st_geometry(albion_true_intersect_footprints_sf), border = "grey40", lwd = 0.4,
     main = "Albion PRE-FILTER QA: TRUE_INTERSECT tiles (191)")
plot(sf::st_geometry(albion_polygon_in_utm12_crs), add = TRUE, border = "red", lwd = 2)

plot(sf::st_geometry(albion_bbox_overlap_footprints_sf), border = "grey40", lwd = 0.4,
     main = "Albion PRE-FILTER QA: BBOX_OVERLAP tiles (364)")
plot(sf::st_geometry(albion_polygon_in_utm12_crs), add = TRUE, border = "red", lwd = 2)

plot(sf::st_geometry(albion_bbox_only_footprints_sf), border = "orange3", lwd = 0.5,
     main = "Albion PRE-FILTER QA: BBOX_ONLY tiles (364 - 191 extras)")
plot(sf::st_geometry(albion_polygon_in_utm12_crs), add = TRUE, border = "red", lwd = 2)

# Intersect looks correct so lets save those

## 5) Save the tile lists you can choose from for filtering

saveRDS(albion_tiles_true_intersect_paths, file.path(albion_qa_log_directory, "qa_keep_tiles_albion_TRUE_INTERSECT_191.rds"))
writeLines(albion_tiles_true_intersect_paths, file.path(albion_qa_log_directory, "qa_keep_tiles_albion_TRUE_INTERSECT_191.txt"))


albion_true_intersect_counts_by_flight <- utm12_tile_header_inventory_table[
  tile_path %in% albion_tiles_true_intersect_paths,
  .(n_tiles = .N),
  by = flight_folder_name
][order(-n_tiles)]

print(albion_true_intersect_counts_by_flight)

saveRDS(albion_true_intersect_counts_by_flight, file.path(albion_qa_log_directory, "qa_albion_true_intersect_counts_by_flight.rds"))

cat("\nPRE-FILTER QA complete. Choose which list to filter from:\n")
cat(" - TRUE_INTERSECT:", file.path(albion_qa_log_directory, "qa_keep_tiles_albion_TRUE_INTERSECT_191.rds"), "\n")

#### Step 7 Filter Pathway #####

albion_filtered_output_directory <- file.path(albion_run_root_directory, "01_filtered")
albion_log_directory             <- file.path(albion_run_root_directory, "logs")

dir.create(albion_filtered_output_directory, recursive = TRUE, showWarnings = FALSE)
dir.create(albion_log_directory, recursive = TRUE, showWarnings = FALSE)

#### Load Intersect Tiles ####

albion_qa_keep_list_true_intersect_rds_path <- file.path(
  albion_log_directory,
  "qa_keep_tiles_albion_TRUE_INTERSECT_191.rds"
)

if (!file.exists(albion_qa_keep_list_true_intersect_rds_path)) {
  stop("QA keep list not found: ", albion_qa_keep_list_true_intersect_rds_path)
}

albion_tile_paths_selected_for_filtering <- readRDS(albion_qa_keep_list_true_intersect_rds_path)

if (length(albion_tile_paths_selected_for_filtering) == 0) {
  stop("QA keep list is empty. Cannot filter.")
}

# Confirm all inputs exist on disk (prevents mid-run failures)
albion_missing_input_tiles_logical <- !file.exists(albion_tile_paths_selected_for_filtering)

if (any(albion_missing_input_tiles_logical)) {
  missing_paths <- albion_tile_paths_selected_for_filtering[albion_missing_input_tiles_logical]
  writeLines(missing_paths, file.path(albion_log_directory, "albion_missing_input_tile_paths.txt"))
  stop("Some Albion input tiles do not exist on disk. See: albion_missing_input_tile_paths.txt")
}

cat("Albion tiles selected for filtering (QA TRUE_INTERSECT):", length(albion_tile_paths_selected_for_filtering), "\n")

## Save a “locked” copy of what this run used (reproducibility)
saveRDS(
  albion_tile_paths_selected_for_filtering,
  file.path(albion_log_directory, "albion_tile_paths_used_for_filtering_TRUE_INTERSECT_191.rds")
)

#### Skip-if-already-filtered Safegaurd #####

albion_existing_filtered_output_files <- list.files(
  path = albion_filtered_output_directory,
  pattern = "\\.(las|laz)$",
  full.names = FALSE,
  ignore.case = TRUE
)

albion_input_tile_stems <- tools::file_path_sans_ext(basename(albion_tile_paths_selected_for_filtering))

albion_tile_already_filtered_logical <- vapply(albion_input_tile_stems, function(single_stem) {
  any(startsWith(albion_existing_filtered_output_files, paste0(single_stem, "_")))
}, logical(1))

albion_tile_paths_remaining_to_filter <- albion_tile_paths_selected_for_filtering[!albion_tile_already_filtered_logical]

cat("Albion tiles already filtered in Albion folder:", sum(albion_tile_already_filtered_logical), "\n")
cat("Albion tiles remaining to filter:", length(albion_tile_paths_remaining_to_filter), "\n")

saveRDS(
  albion_tile_paths_remaining_to_filter,
  file.path(albion_log_directory, "albion_tile_paths_remaining_to_filter_TRUE_INTERSECT_191.rds")
)

if (length(albion_tile_paths_remaining_to_filter) == 0) {
  message("All QA-approved Albion tiles already have filtered outputs. Skipping filtering.")
} else {

#### Build Catalog ####  
  albion_las_catalog_for_filtering <- lidR::readLAScatalog(albion_tile_paths_remaining_to_filter)

  # One output per input tile (no chunking)
  lidR::opt_chunk_size(albion_las_catalog_for_filtering)   <- 0
  lidR::opt_chunk_buffer(albion_las_catalog_for_filtering) <- 0

  # Unique output naming to avoid collisions
  lidR::opt_output_files(albion_las_catalog_for_filtering) <- file.path(
    albion_filtered_output_directory,
    "{ORIGINALFILENAME}_{ID}_filtered"
  )

  # Parallel settings (stable)
  options(lidR.cores = 8)
  lidR::set_lidr_threads(1)

#### Albion Filter Function (later we will use one function and apply to both areas) ####
  
   albion_filter_function <- function(x, ...) {
    tryCatch({

      if (inherits(x, "LAScluster")) x <- lidR::readLAS(x)
      if (is.null(x) || lidR::is.empty(x)) return(NULL)

      x <- lidR::filter_poi(x, !Withheld_flag & !Synthetic_flag)
      x <- lidR::classify_noise(x, lidR::ivf(res = 4, n = 12))
      x <- lidR::filter_poi(x, !Classification %in% c(7L, 18L))

      if (lidR::is.empty(x)) return(NULL)
      x

    }, error = function(e) {

      cat(
        format(Sys.time(), "%Y-%m-%d %H:%M:%S"),
        "- Albion filter ERROR:",
        conditionMessage(e),
        "\n",
        file = file.path(albion_log_directory, "albion_filter_errors.txt"),
        append = TRUE
      )

      NULL
    })
   }
  
#### Step 8 Run Filter #####
  
   albion_filter_apply_result <- lidR::catalog_apply(albion_las_catalog_for_filtering, albion_filter_function)
  print(albion_filter_apply_result)

  cat("Albion filtering complete. Outputs in:\n", albion_filtered_output_directory, "\n")
}
  
#### Step 9 Post-run QA ####

albion_filtered_output_tile_paths <- list.files(
  path = albion_filtered_output_directory,
  pattern = "\\.(las|laz)$",
  full.names = TRUE,
  ignore.case = TRUE
)

cat("Albion filtered outputs present:", length(albion_filtered_output_tile_paths), "\n")

saveRDS(
  albion_filtered_output_tile_paths,
  file.path(albion_log_directory, "albion_filtered_output_tile_paths_TRUE_INTERSECT_191.rds")
)  

```

Albions tiles are saved and filtered. We also should verify that the Elko runs didn't have issues since they had 2 CRS listed

```{r Check Elko Tiles}
### Elko had mixed CRS so we want to make sure they were all included in the filter ####

## CHECK 1: Total Elko tiles by CRS group (header-only) to confirm ElkoNW_LAZ is split and how many tiles are in each CRS

elko_tile_inventory_table <- lidar_readable_tiles_table[
  flight_folder_name == "ElkoNW_LAZ"
]

elko_tile_counts_by_crs_table <- elko_tile_inventory_table[
  , .(n_tiles = .N),
  by = .(crs_wkt_prefix, crs_wkt_length)
][order(-n_tiles)]

print(elko_tile_counts_by_crs_table)

## CHECK 2: Were any Elko tiles excluded from keep_files?
## Purpose:
##   - Compare total Elko tiles vs Elko tiles selected for processing
##   - If excluded_count > 0, tiles were dropped by selection logic

# Load the keep list you used for the successful run
study_area1_keep_tile_paths <- readRDS(file.path(pipeline_log_directory, "keep_files.rds"))

# Elko tiles that exist vs Elko tiles that were selected
elko_all_tile_paths <- elko_tile_inventory_table$tile_path
elko_selected_tile_paths <- intersect(elko_all_tile_paths, study_area1_keep_tile_paths)

cat("Elko total tiles in folder:", length(elko_all_tile_paths), "\n")
cat("Elko tiles included in keep_files:", length(elko_selected_tile_paths), "\n")
cat("Elko tiles excluded:", length(setdiff(elko_all_tile_paths, study_area1_keep_tile_paths)), "\n")

# Save excluded list for reproducibility
elko_excluded_tile_paths <- setdiff(elko_all_tile_paths, study_area1_keep_tile_paths)

saveRDS(
  elko_excluded_tile_paths,
  file.path(pipeline_log_directory, "elko_tiles_excluded_from_keep_files.rds")
)
writeLines(
  elko_excluded_tile_paths,
  file.path(pipeline_log_directory, "elko_tiles_excluded_from_keep_files.txt")
)

## CHECK 3: Do excluded Elko tiles intersect South Hills boundary?
## Purpose: determine if exclusions are “real” or a CRS/selection artifact

south_hills_polygon_feature <- study_area_polygon_rows[study_area_polygon_rows$Section == "South Hills", ]

# Use the TARGET CRS for the AOI transform (same one you used in run)
south_hills_polygon_in_target_crs <- st_transform(south_hills_polygon_feature, crs_lidar)

# Build footprints for excluded Elko tiles
elko_excluded_tile_paths <- readRDS(file.path(pipeline_log_directory, "elko_tiles_excluded_from_keep_files.rds"))

elko_excluded_footprint_table <- rbindlist(lapply(elko_excluded_tile_paths, function(single_tile_path) {
  h <- lidR::readLASheader(single_tile_path)
  data.table(
    tile_path = single_tile_path,
    xmin = h@PHB[["Min X"]],
    xmax = h@PHB[["Max X"]],
    ymin = h@PHB[["Min Y"]],
    ymax = h@PHB[["Max Y"]]
  )
}))

elko_excluded_footprint_sf <- st_as_sf(elko_excluded_footprint_table, coords=c("xmin","ymin"), crs=crs_lidar)
elko_excluded_footprint_sf$geometry <- st_sfc(lapply(seq_len(nrow(elko_excluded_footprint_table)), function(i) {
  st_polygon(list(matrix(
    c(elko_excluded_footprint_table$xmin[i], elko_excluded_footprint_table$ymin[i],
      elko_excluded_footprint_table$xmax[i], elko_excluded_footprint_table$ymin[i],
      elko_excluded_footprint_table$xmax[i], elko_excluded_footprint_table$ymax[i],
      elko_excluded_footprint_table$xmin[i], elko_excluded_footprint_table$ymax[i],
      elko_excluded_footprint_table$xmin[i], elko_excluded_footprint_table$ymin[i]),
    ncol=2, byrow=TRUE
  )))
}), crs=crs_lidar)

excluded_tiles_that_should_intersect <- lengths(st_intersects(elko_excluded_footprint_sf, south_hills_polygon_in_target_crs)) > 0

cat("Excluded Elko tiles that actually intersect South Hills:", sum(excluded_tiles_that_should_intersect), "\n")

```

## Classification

We now have the South Hills and Albions as separate files containing their own filtered products - though file naming scheme was kept consistent. We can now classify both of these.

```{r Full Area - Classification}

##### 0) Define directories FIRST #####

# Albion (assumes albion_run_root_directory + albion_filtered_output_directory already exist in your session)
albion_ground_output_directory <- file.path(albion_run_root_directory, "02_ground_csf")
albion_log_directory           <- file.path(albion_run_root_directory, "logs")

# South Hills
southhills_run_root_directory        <- "R:/CACR_HSM_LiDAR/Output_Files/FullArea_Run"
southhills_filtered_output_directory <- file.path(southhills_run_root_directory, "01_filtered")
southhills_ground_output_directory   <- file.path(southhills_run_root_directory, "02_ground_csf")
southhills_log_directory             <- file.path(southhills_run_root_directory, "logs")

dir.create(albion_ground_output_directory,     recursive = TRUE, showWarnings = FALSE)
dir.create(albion_log_directory,               recursive = TRUE, showWarnings = FALSE)
dir.create(southhills_ground_output_directory, recursive = TRUE, showWarnings = FALSE)
dir.create(southhills_log_directory,           recursive = TRUE, showWarnings = FALSE)


##### 1) Pre-Classification Checks #####

albion_filtered_files <- list.files(
  albion_filtered_output_directory,
  pattern = "\\.(las|laz)$",
  full.names = TRUE,
  ignore.case = TRUE
)

southhills_filtered_files <- list.files(
  southhills_filtered_output_directory,
  pattern = "\\.(las|laz)$",
  full.names = TRUE,
  ignore.case = TRUE
)

stopifnot(length(albion_filtered_files) > 0)
stopifnot(length(southhills_filtered_files) > 0)

stopifnot(all(file.exists(albion_filtered_files)))
stopifnot(all(file.exists(southhills_filtered_files)))

header_ok <- function(f) {
  h <- try(lidR::readLASheader(f), silent = TRUE)
  !inherits(h, "try-error")
}

stopifnot(all(vapply(albion_filtered_files[1:min(10, length(albion_filtered_files))], header_ok, logical(1))))
stopifnot(all(vapply(southhills_filtered_files[1:min(10, length(southhills_filtered_files))], header_ok, logical(1))))

cat("Albion existing CSF outputs:",
    length(list.files(albion_ground_output_directory, pattern="\\.(las|laz)$", ignore.case=TRUE)), "\n")
cat("South Hills existing CSF outputs:",
    length(list.files(southhills_ground_output_directory, pattern="\\.(las|laz)$", ignore.case=TRUE)), "\n")


##### 2) Define CSF algorithm object #####

csf_ground_fullArea <- lidR::csf(
  sloop_smooth     = TRUE,
  class_threshold  = 0.5,
  cloth_resolution = 0.5,
  rigidness        = 2
)


##### 3) Study area run list #####

study_area_run_list <- list(
  list(
    study_area_name = "Albion",
    filtered_dir    = albion_filtered_output_directory,
    ground_dir      = albion_ground_output_directory,
    log_dir         = albion_log_directory
  ),
  list(
    study_area_name = "SouthHills",
    filtered_dir    = southhills_filtered_output_directory,
    ground_dir      = southhills_ground_output_directory,
    log_dir         = southhills_log_directory
  )
)


##### 4) Helper: list filtered tiles #####

list_filtered_tile_paths <- function(filtered_directory_path) {
  list.files(
    path = filtered_directory_path,
    pattern = "\\.(las|laz)$",
    full.names = TRUE,
    ignore.case = TRUE
  )
}


##### 5) Helper: detect valid CSF outputs (resume-safe) #####
## DONE means:
##  - matching output exists (stem_csf OR stem_<anything>_csf)
##  - AND output header is readable (so partial/broken files get re-run)

detect_valid_csf_outputs <- function(filtered_tile_paths, ground_dir) {

  ground_outputs_full <- list.files(
    ground_dir,
    pattern = "\\.(las|laz)$",
    full.names = TRUE,
    ignore.case = TRUE
  )

  if (length(ground_outputs_full) == 0) {
    return(rep(FALSE, length(filtered_tile_paths)))
  }

  ground_outputs_base <- basename(ground_outputs_full)
  filtered_stems      <- tools::file_path_sans_ext(basename(filtered_tile_paths))

  has_valid_output <- vapply(filtered_stems, function(stem) {

    # Escape regex metacharacters in stem
    stem_escaped <- gsub("([\\.^$|()\\[\\]{}*+?\\\\])", "\\\\\\1", stem)

    # Match:
    #   ^stem_csf.las
    #   ^stem_<anything>_csf.las
    pattern <- paste0("^", stem_escaped, "(_.*)?_csf\\.(las|laz)$")

    idx <- which(grepl(pattern, ground_outputs_base, ignore.case = TRUE))
    if (length(idx) == 0) return(FALSE)

    # At least one candidate must be readable
    any(vapply(ground_outputs_full[idx], function(out_path) {
      h <- try(lidR::readLASheader(out_path), silent = TRUE)
      !inherits(h, "try-error")
    }, logical(1)))

  }, logical(1))

  has_valid_output
}


##### 6) Helper: build CSF worker function #####

make_csf_ground_function <- function(study_area_name, log_directory_path, csf_algorithm_object) {

  dir.create(log_directory_path, recursive = TRUE, showWarnings = FALSE)
  csf_error_log_path <- file.path(log_directory_path, paste0(study_area_name, "_csf_ground_errors.txt"))

  function(cluster, ...) {
    tryCatch({

      las_object <- lidR::readLAS(cluster)
      if (is.null(las_object) || lidR::is.empty(las_object)) return(NULL)

      las_object <- lidR::classify_ground(las_object, csf_algorithm_object)

      las_object

    }, error = function(e) {

      cat(
        format(Sys.time(), "%Y-%m-%d %H:%M:%S"),
        "- ", study_area_name, " CSF ERROR:",
        conditionMessage(e),
        "\n",
        file = csf_error_log_path,
        append = TRUE
      )

      NULL
    })
  }
}


##### 7) MAIN LOOP: resume where it left off #####

for (single_run in study_area_run_list) {

  study_area_name <- single_run$study_area_name
  filtered_dir    <- single_run$filtered_dir
  ground_dir      <- single_run$ground_dir
  log_dir         <- single_run$log_dir

  cat("\n==============================\n")
  cat("Running CSF ground classification for:", study_area_name, "\n")
  cat("Filtered inputs:", filtered_dir, "\n")
  cat("Ground outputs :", ground_dir, "\n")
  cat("==============================\n\n")

  filtered_tile_paths <- list_filtered_tile_paths(filtered_dir)

  if (length(filtered_tile_paths) == 0) {
    warning("No filtered tiles found for ", study_area_name, " in: ", filtered_dir)
    next
  }

  cat(study_area_name, "filtered tiles found:", length(filtered_tile_paths), "\n")

  # Resume-safe skip logic
  already_done_logical <- detect_valid_csf_outputs(filtered_tile_paths, ground_dir)
  remaining_tile_paths <- filtered_tile_paths[!already_done_logical]

  cat(study_area_name, "tiles already ground-classified (valid):", sum(already_done_logical), "\n")
  cat(study_area_name, "tiles remaining to classify            :", length(remaining_tile_paths), "\n")

  saveRDS(
    remaining_tile_paths,
    file.path(log_dir, paste0(study_area_name, "_remaining_tiles_to_csf.rds"))
  )

  if (length(remaining_tile_paths) == 0) {
    message("All tiles already have valid CSF outputs for ", study_area_name, ". Skipping.")
    next
  }

  ctg_remaining <- lidR::readLAScatalog(remaining_tile_paths)
  lidR::opt_chunk_size(ctg_remaining)   <- 0
  lidR::opt_chunk_buffer(ctg_remaining) <- 0

  # Keep naming consistent with existing outputs like:
  # USGS_..._filtered_260_csf.las
  lidR::opt_output_files(ctg_remaining) <- file.path(
    ground_dir,
    "{ORIGINALFILENAME}_{ID}_csf"
  )

  options(lidR.cores = 8)
  lidR::set_lidr_threads(1)

  csf_worker_function <- make_csf_ground_function(
    study_area_name      = study_area_name,
    log_directory_path   = log_dir,
    csf_algorithm_object = csf_ground_fullArea
  )

  csf_apply_result <- lidR::catalog_apply(ctg_remaining, csf_worker_function)
  print(csf_apply_result)

  cat("\nCSF classification finished for ", study_area_name, ". Outputs in:\n", ground_dir, "\n")
}


##### 8) Sanity checks #####

cat("\n--- FINAL OUTPUT COUNTS ---\n")
cat("South Hills CSF outputs:",
    length(list.files(southhills_ground_output_directory, pattern="\\.(las|laz)$", ignore.case=TRUE)), "\n")
cat("Albion CSF outputs     :",
    length(list.files(albion_ground_output_directory, pattern="\\.(las|laz)$", ignore.case=TRUE)), "\n")

# Confirm one South Hills output has ground class points
one_out <- list.files(southhills_ground_output_directory, pattern="\\.(las|laz)$", full.names=TRUE, ignore.case=TRUE)[1]
if (!is.na(one_out) && file.exists(one_out)) {
  las_chk <- lidR::readLAS(one_out)
  print(table(las_chk$Classification))
}


list.files(southhills_ground_output_directory, pattern="\\.(las|laz)$", full.names=FALSE) |> length()
list.files(albion_ground_output_directory,     pattern="\\.(las|laz)$", full.names=FALSE) |> length()

one_out <- list.files(southhills_ground_output_directory, pattern="\\.(las|laz)$", full.names=TRUE)[1]
las_chk <- lidR::readLAS(one_out)
table(las_chk$Classification)
```

